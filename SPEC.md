# Спецификация языка

Заполните этот файл описанием вашего языка.

## 1. Название и идея языка

Название:

Краткое описание:

Для чего предназначен язык:

Пример простой программы:

```text
// Замените пример на синтаксис вашего языка
print("Hello, world!");
```

## 2. Лексика

Опишите лексические элементы языка.

### Идентификаторы

Пример:

```text
name
counter
total_sum
```

Правила:

- TODO

### Литералы

Числа:

```text
123
3.14
```

Строки:

```text
"hello"
```

Булевы значения:

```text
true
false
```

### Ключевые слова

Список ключевых слов:

```text
TODO
```

### Операторы

Список операторов:

```text
TODO
```

### Комментарии

Пример:

```text
// TODO
```

## 3. Грамматика

Опишите грамматику языка в BNF, EBNF или похожей форме.

Пример структуры:

```ebnf
program     = statement* ;
statement   = var_decl | assignment | if_stmt | while_stmt | func_decl | return_stmt | expr_stmt ;
var_decl    = "let" IDENT "=" expression ";" ;
assignment  = IDENT "=" expression ";" ;
if_stmt     = "if" "(" expression ")" block ("else" block)? ;
while_stmt  = "while" "(" expression ")" block ;
func_decl   = "fn" IDENT "(" parameters? ")" block ;
return_stmt = "return" expression? ";" ;
block       = "{" statement* "}" ;
expression  = equality ;
```

Замените пример на реальную грамматику вашего языка.

## 4. Типы данных

Опишите поддерживаемые типы.

| Тип | Примеры значений | Описание |
|---|---|---|
| TODO | TODO | TODO |

Опишите, является ли типизация статической или динамической.

## 5. Выражения и операторы

Опишите:

- арифметические операции;
- операции сравнения;
- логические операции;
- приоритет операторов;
- правила вычисления выражений.

Таблица приоритетов:

| Приоритет | Операторы | Ассоциативность |
|---:|---|---|
| TODO | TODO | TODO |

## 6. Переменные и области видимости

Опишите:

- как объявляется переменная;
- можно ли менять значение переменной;
- где переменная видима;
- что происходит при обращении к неизвестной переменной;
- можно ли затенять переменные во вложенных блоках.

Пример:

```text
TODO
```

## 7. Управляющие конструкции

Опишите условные операторы, циклы и блоки.

### Условие

```text
TODO
```

### Цикл

```text
TODO
```

### break и continue

Поддерживаются ли `break` и `continue`:

```text
TODO
```

## 8. Функции

Опишите:

- как объявляется функция;
- как передаются параметры;
- как возвращается значение;
- есть ли рекурсия;
- как устроены локальные переменные.

Пример:

```text
TODO
```

## 9. Ввод и вывод

Опишите доступные операции ввода/вывода.

Пример:

```text
TODO
```

## 10. Ошибки

Опишите, какие ошибки обнаруживает язык и как они показываются пользователю.

| Ошибка | Пример программы | Ожидаемое сообщение |
|---|---|---|
| Синтаксическая ошибка | TODO | TODO |
| Ошибка типа | TODO | TODO |
| Неизвестная переменная | TODO | TODO |
| Ошибка времени выполнения | TODO | TODO |

## 11. Модель выполнения

Опишите, как выполняется программа:

- с чего начинается выполнение;
- как выполняются инструкции;
- как вычисляются выражения;
- как работает стек вызовов функций;
- что происходит при ошибке.

## 12. Ограничения языка

Перечислите известные ограничения.

Пример:

- нет модулей;
- нет пользовательских структур;
- нет оптимизаций;
- строки не поддерживают escape-последовательности.

