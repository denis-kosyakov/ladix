# Манифест демонстрационных программ

Каталог `examples/` содержит 9 программ на Ladix: семь обязательных (закрывают чеклист
«Примеры») и две бизнес-демонстрации. **Обязательные 7** — эталон текущего ядра (v1):
исполняются один-в-один через `ladix run`, ожидаемый вывод проверяется как golden-фикстура.
**Бизнес-демонстрации** показывают декларативно-процессный синтаксис (`источник`, `метрика`,
`процесс`, триггер `когда`) и в ядре v1 ещё НЕ исполняются — подробности в разделе ниже.
Подробнее о форматах ошибок — §13; о CLI-командах — README, раздел CLI.

Общие соглашения для всех файлов:

- Кодировка UTF-8; русские идентификаторы и ключевые слова (§2).
- Отступы значащие, 4 пробела на уровень, табы запрещены (§2).
- Комментарии — `#` до конца строки (§2).
- Вывод только через `печать(...)`: аргументы через один пробел, в конце перевод строки.
  `Дробное` печатается с точкой (`1.0`, `3.4`, `2175.0`), `Список` — `[элементы через , ]`.
- Запуск **обязательных 7**: `ladix run examples/<файл>.ladix`, код возврата `0`
  (кроме `ошибка.ladix` — код `1`). Бизнес-демонстрации в ядре v1 завершаются кодом `1`:
  `онбординг.ladix` — в **рантайме** на `запустить процесс` (исполнение процессов — фича 006),
  `выручка.ladix` — на стадии парсера (триггер `когда` — фича 007); это ожидаемо, см. раздел ниже.

## Обязательные 7

| Файл | Назначение | Golden-stdout |
|---|---|---|
| `hello.ladix` | вывод (`печать`) | `Привет, Уклад!` |
| `арифметика.ladix` | арифметика и логика с приоритетами, переменные | `14 20` / `3 2` / `3.4` / `25` / `истина` / `истина` |
| `условие.ladix` | `если`/`иначе если`/`иначе`, области видимости | `категория: средний` / `чётная сумма` |
| `цикл.ladix` | `пока` и `для`, `прервать`/`продолжить` | `сумма чётных: 30` / `дорогие: [250, 400]` / `первая степень двойки > 16: 32` |
| `функция.ladix` | функция с параметрами и возвратом, ранний возврат | `2175.0` / `0` |
| `факториал.ladix` | рекурсия с базовым случаем | `120` / `1` |
| `ошибка.ladix` | понятная ошибка со строкой и колонкой | см. ниже (stderr, код возврата 1) |

Golden-stdout записан построчно (символ `/` — разделитель строк вывода, в файле это переводы строк).

### `ошибка.ladix` — точная диагностика

Программа синтаксически и семантически корректна, падает в рантайме. Проверяется **stderr** и
**код возврата 1** (§13). Деление на ноль — runtime-ошибка (правило продублировано в §4 и §13).

- Категория: деление на ноль (runtime).
- Позиция: строка 5, колонка 14 (счёт колонок с 1; колонка 14 указывает на оператор `/`).
- Код возврата: 1.

Эталонный stderr (двухстрочный формат):

```
Ошибка в строке 5, колонка 14:
деление на ноль
```

Опционально (если имплементация печатает исходную строку с кареткой — не обязательно по
контракту §13): после двух строк выводится строка-источник и `^` под оператором `/`.

## Бизнес-демонстрации (синтаксис v2 — в ядре v1 не исполняются)

> **Статус в v1.** Эти две программы демонстрируют декларативно-процессную часть Ladix —
> `источник`, `метрика`, `процесс`/`шаг`, триггер `когда`. Ядро уже разбирает объявления
> `источник`/`метрика` (фича 004) и `процесс`/`шаг` (фича 005), но судьба двух демо разная:
>
> - `онбординг.ladix` теперь **парсится и проходит семантику чисто**; `ladix run` завершается
>   **кодом 1** в **рантайме** на top-level `запустить процесс` (ожидаемая диагностика
>   «конструкция запустить процесс не поддерживается в этой версии», зафиксирована тестом
>   `cmd/ladix/main_test.go`). Исполнение процессов — фича **006** (движок процессов).
> - `выручка.ladix` **остаётся** парс-ошибкой с **кодом 1**, но ожидаемый токен сдвинулся с
>   `источник`/`процесс` на **`когда`** (диагностика «неожиданный токен `когда`», зафиксирована
>   тестом `parser/examples_test.go`). Триггеры `когда` — фича **007**.
>
> Строгих golden-фикстур у них нет и в v1 не предполагается; файлы оставлены как опережающая
> демонстрация целевого синтаксиса.

| Файл | Назначение | Golden-stdout |
|---|---|---|
| `выручка.ladix` | источник + метрика + триггер «метрика → процесс» | демо, вывод дата-зависим, строгой golden нет |
| `онбординг.ladix` | процесс с человеком-в-цикле, передача данных между шагами | демо, вывод id-зависим, строгой golden нет |

`выручка.ladix` использует фикстуру `data/sales.json` (в корне репозитория). Вывод зависит от
системной даты (`период: ежемесячно` берёт окно от `сегодня()`), поэтому строгой stdout-фикстуры
нет; числовую корректность метрики проверяют unit-тесты движка с фиксированной датой. Канал
проверки метрики отдельно от триггера — команда `metric` (README, раздел CLI).

`онбординг.ladix` детерминирован по логике (создаётся инстанс, авто-шаг `завести_доступы`
отрабатывает сразу, процесс упирается в человеческий шаг → статус `ожидает`), но будет печатать
ID инстанса (деталь реализации). Строгая фикстура должна маскировать ID (проверять префикс
`запущен онбординг, id:`). Жизненный цикл процессов проверяют unit-тесты движка со Store;
подробнее — docs/execution-model.md.
