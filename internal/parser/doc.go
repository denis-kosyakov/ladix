// Package parser — ручной recursive-descent разборщик Ladix: потребляет поток
// токенов лексера (фича 001) и строит AST императивного ядра (подмножество B),
// накапливая синтаксические ошибки в общий errors.ErrorList.
//
// Граф зависимостей: parser → {ast, errors, lexer} (без циклов). Состояния на
// уровне пакета нет — Parser создаётся явно (New), курсор и флаг panic-mode —
// поля экземпляра (FR-029, guardrail 9). Каскад из одной функции на уровень
// приоритета, один токен предпросмотра, без бэктрекинга и без генераторов
// парсеров/regex (конституция II).
//
// Границы: парсер НЕ выполняет eval и НЕ делает семантику (типы/области/арность/
// контекст вернуть·прервать·продолжить). Единственная структурная проверка —
// вложенная функция (синтаксис, grammar §4). Декларации источник/метрика/
// процесс/когда и значение/{ } на верхнем уровне дают «неожиданный элемент».
// См. specs/002-parser-ast/contracts/{ast,syntax-errors}.md.
package parser
