# Чеклист качества реализации: Финализация продукта v1 Ladix

**Назначение**: гейт готовности РЕАЛИЗАЦИИ финализации — проверяемый барьер перед закрытием `implement`. Это НЕ дубль [requirements.md](requirements.md) (тот валидирует качество самой спецификации) и НЕ простановка `CHECKLIST.md` (рубрика готовности продукта). Здесь проверяется, что фактическая реализация фичи 009 выполнена полно, детерминированно и без выхода за скоуп.

**Создан**: 2026-06-15
**Фича**: [spec.md](../spec.md)
**Якорь**: `docs/v1-finalization-model.md` §VF-0…§VF-8

> Все пункты оставлены НЕотмеченными `- [ ]` намеренно: чеклист — гейт для прогона `implement`, отмечается по факту закрытия. Пути в пунктах — относительно корня репозитория (`…/ladix/`); модуль и гейты — из `src/`.

## Воспроизводимость README (US1, FR-001..FR-005, SC-001/SC-002)

- [ ] CHK001 README из корня собирается дословно блок-за-блоком, exit 0 на каждом блоке; ни одного не-упомянутого ручного `cd src` [US1, FR-001/FR-003]
- [ ] CHK002 Введён ОДИН канонический мост сборки `cd src && go build -o ../ladix ./cmd/ladix`; он же проведён сквозь все команды запуска (`run`/`serve`/`emit`/`metric`) [US1, FR-002, T005]
- [ ] CHK003 Запуск `./ladix` идёт из корня (ради `data/`, `data/sales.json`, `examples/`); все команды запуска приведены к `./ladix run examples/… ` [US1, FR-001/FR-002]
- [ ] CHK004 `./ladix run examples/hello.ladix` печатает ровно `Привет, Уклад!`, exit 0 [US1, FR-003, SC-001]
- [ ] CHK005 Блок тестов оформлен как `cd src && go test ./...` и даёт 10/10 пакетов `ok`, ноль «no test files» [US1, FR-003, SC-002]
- [ ] CHK006 `src/README.md` содержит рабочий dev-блок ИЛИ явную отсылку к корневому README — не голую заглушку «Переехало» [US1, FR-004, T006]
- [ ] CHK007 Smoke-замок воспроизводимости R1–R5 присутствует, прогоняет ровно блоки README из корня и зелёный; инверсия рабочая (нерабочая команда README → замок краснеет) [US1, FR-005, T007]

## Честная простановка CHECKLIST.md (US2, FR-006/FR-007, SC-003)

- [ ] CHK008 Все 50 пунктов-чекбоксов `CHECKLIST.md` отмечены `- [x]` (49 кодовых + `hello.lang` по намерению) [US2, FR-006]
- [ ] CHK009 `grep -c '^- \[ \]' CHECKLIST.md` = 0 И `grep -c '^- \[x\]' CHECKLIST.md` = 50 [US2, SC-003, T014]
- [ ] CHK010 Каждый `[x]` имеет верифицируемое доказательство файл:строка — строка существует и содержит заявленный факт (выборочно подтверждено ≥3) [US2, FR-007]
- [ ] CHK011 Пункт «Есть `examples/hello.lang`» закрыт `[x]` со сноской `.lang→.ladix` (реальный файл `examples/hello.ladix`) [US2, FR-006, T012]
- [ ] CHK012 Текст пунктов рубрики НЕ изменён — diff против исходника показывает только чекбоксы и сноску `hello.lang` [US2, FR-006, T014]
- [ ] CHK013 Пункты с доказательством-прогоном (рекурсия, строка/колонка) пинятся фикс-входом/фикс-Clock, не зависят от текущей даты [US2, spec §US2.4]

## Полная витрина примеров (US3, FR-008..FR-011, SC-004/SC-005)

- [ ] CHK014 Создано 7 примеров: `событие`, `расписание`, `метрики` (≥2 источника + ≥2 метрики), `процесс` (полный цикл), `ошибка_синтаксис`, `ошибка_тип`, `ошибка_процесс` `.ladix` [US3, FR-008, T015–T025]
- [ ] CHK015 Каждый позитив закрыт golden-замком в `cmd/ladix`: байт-точный stdout, либо exit+маркеры для дата-зависимых [US3, FR-009, T016/T018/T020/T022]
- [ ] CHK016 Полный цикл процесса (`run --db`→`tasks`→`complete`→пробуждение) пинится через DB-aware run-golden с `maskIDs` (`p-NNNNNN`/`t-NNNNNN`, метки времени) в `t.TempDir()`; байт-точно в любой день [US3, spec §US3.4, T022]
- [ ] CHK017 Три негатива утверждают КОНКРЕТНЫЙ класс stderr (синтаксис/тип/процесс — не общий `Ошибка в строке`) + двухстрочный канон `строка N, колонка M` + код выхода 1; без Go stack trace [US3, FR-009, T026]
- [ ] CHK018 Инверсия негативов рабочая: пустой stderr ИЛИ exit≠1 ИЛИ потеря класса ошибки → замок краснеет [US3, T026]
- [ ] CHK019 `выручка.ladix` имеет отдельный run-golden с фикс-Clock/фикс-данными во `t.TempDir()` — байт-точный stdout, зелёный в любой день; сам `выручка.ladix` пинится по exit 0 [US3, FR-011, T027]
- [ ] CHK020 `examples/MANIFEST.md` обновлён: запись (файл / назначение / golden-stdout либо пометка дата-зависимости) на каждый новый пример + честное пояснение, почему на `выручка.ladix` нет строгой stdout-фикстуры [US3, FR-009, T028]
- [ ] CHK021 `TestExamplesParseCleanSet` зелёный: позитивы добавлены в parse-clean-сет, негативы НЕ добавлены (должны падать) [US3, FR-010, T020]

## Пользовательский quickstart (US4, FR-012/FR-013, SC-006)

- [ ] CHK022 `docs/quickstart.md` существует (русский, пользовательский, не архитектурный): установка/сборка → первая программа → первая метрика → первый процесс [US4, FR-012, T029]
- [ ] CHK023 Все команды quickstart — из канонического набора US1 (`-o ../ladix`, `./ladix` из корня) и воспроизводимы из чистого клона, exit 0 на каждом шаге [US4, FR-013, T030]
- [ ] CHK024 Путь «метрика + процесс» проходится без обращения к архитектурным `*-model.md`; дата-зависимые шаги помечены «вывод зависит от даты» (пиннинг exit, не stdout) [US4, SC-006, T029]
- [ ] CHK025 Шаги quickstart покрыты smoke-каналом (расширение T007) или отдельным замком — exit 0 на каждом шаге [US4, FR-013, T030]

## Выравнивание документации под код (US5, FR-014..FR-019, SC-007)

- [ ] CHK026 «Go 1.22+» → фактический минимум `go 1.25` (`src/go.mod:3`) в README/ARCHITECTURE; grep `1.22` в командах сборки = 0 [US5, FR-014, T031]
- [ ] CHK027 Обещание «Найдено K ошибок.» снято/переписано в SPEC §13 и README под фактическое накопление блоков без итоговой строки; замок `aggregate_test.go` НЕ тронут и зелёный [US5, FR-015, T032]
- [ ] CHK028 Структура тестов задокументирована (README §9): реальные тесты в `src/internal/*/*_test.go`, запуск `cd src && go test ./...`; `tests/`-заглушка остаётся честной отсылкой [US5, FR-016, T033]
- [ ] CHK029 Стейл-коммент `онбординг.ladix:13` («deferred до 006») переписан под фактическое исполнение движком 006 и согласован с `MANIFEST.md`; grep `deferred до 006` = 0 [US5, FR-017, T034]
- [ ] CHK030 `тип(x)` помечен «зарезервировано v1» в `docs/stdlib.md`/`SPEC.md`/`docs/eval-model.md`; инвариант «РОВНО 35 builtins» получил честную сноску о достижимости 34/35 [US5, FR-018, T035]
- [ ] CHK031 `тип(x)` НЕ активирован: регистрация/реализация не удалены, `печать(тип(5))` остаётся reserved-word ошибкой (поведение кода не менялось) [US5, FR-018, T036]
- [ ] CHK032 Grep-инвариант US5 = 0 совпадений по всем снятым утверждениям (`1.22` в сборке, «Найдено K ошибок», «deferred до 006», доступность `тип(x)` в v1) [US5, FR-019, SC-007, T036]

## Приёмка и гейты (US6, FR-020, SC-008)

- [ ] CHK033 `cd src && go build ./... && go vet ./... && go test ./... -count=1` — build/vet PASS, тесты 10/10 пакетов `ok`, exit 0 [US6, SC-008, T038]
- [ ] CHK034 Самооценка рубрики §1–§9 заполнена: каждый из 9 пунктов закрыт доказательством файл:строка или ссылкой на зелёный замок [US6, FR-020, T037]
- [ ] CHK035 Финальный smoke из корня чистого клона (блоки README R1–R5 + шаги quickstart) — каждый блок exit 0; `hello` → `Привет, Уклад!` [US6, SC-001, T039]
- [ ] CHK036 Рабочее дерево чистое: `git status` без незакоммиченного собранного бинарника; `/ladix` и `/src/ladix` в `.gitignore` [US6, SC-008, T040/T001]
- [ ] CHK037 Никакой новой языковой функциональности/бонусов/v2: 0 строк прод-кода языка, 0 новых зависимостей/грамматики/builtins; операторная арифметика дат/длительностей остаётся deferred v2 [US6, spec «Вне скоупа»]

## Детерминизм (сквозной)

- [ ] CHK038 Ни один golden-замок не пинится по `сегодня()`: каждое дата-зависимое демо построено на фикс-`Clock`/фикс-данных или прогоне во `t.TempDir()` — зелёный в любой день [FR-010/FR-011, SC-005]
- [ ] CHK039 Все недетерминированные ID/метки времени стабилизированы через `maskIDs` (`p-NNNNNN`/`t-NNNNNN`, `<время>`) до байт-точного сравнения [US3, T022]
- [ ] CHK040 Существующий хелпер детерминизма (`writeProgFixture`/`writeTriggerFixture`/`maskIDs`) переиспользован, а не продублирован; новый хелпер добавлен только при доказанной нехватке (напр. `--db <tmp>`), без глобального состояния [Foundational, T004]
