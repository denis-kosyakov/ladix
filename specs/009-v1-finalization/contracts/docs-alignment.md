# Контракт приёмки: выравнивание доков (docs-alignment)

**Что контрактует.** Grep-инварианты доков после выравнивания: каких утверждений
БОЛЬШЕ НЕТ, какие появились, и что поведение кода при этом НЕ изменилось. Источник:
§VF-2, §VF-3, FR-014..019, SC-007.

## Снятые утверждения → grep = 0

| # | Что ищем | Где | Ожидание после выравнивания |
|---|----------|-----|------------------------------|
| A1 | `1.22` | команды сборки README/ARCHITECTURE | 0 совпадений (заменено на `go 1.25`) |
| A2 | `Найдено K ошибок` / «Найдено» как обещанная сводка | SPEC §13, README | 0 совпадений (описано накопление блоков без сводки) |
| A3 | `deferred до 006` | `examples/онбординг.ladix` | 0 совпадений (переписано под исполнение движком 006) |
| A4 | утверждения о доступности `тип(x)` в v1 | `docs/stdlib.md`, SPEC §220/§343, `docs/eval-model.md` | 0 «активна»; помечено «зарезервировано v1» / внесено в §12-список |

## Появившиеся утверждения (должны присутствовать)

| Что | Где | Подтверждается |
|-----|-----|----------------|
| `go 1.25` как минимум | README, ARCHITECTURE | `src/go.mod:3` (`go 1.25.0`) |
| тесты в `src/internal/`, `cd src && go test ./...` | README/§9 | реальное расположение `*_test.go` |
| `тип(x)` «зарезервировано/недоступно в v1» | `docs/stdlib.md` | `src/internal/lexer/keywords.go` (`"тип": true`) |
| сноска достижимости «35 зарегистрировано, 34 достижимо» | `builtins.go`-коммент | `builtins.go` + lexer-резерв |

## Инварианты поведения (НЕ меняются)

- ИНВАРИАНТ: тест-замок отсутствия строки «Найдено» (`src/internal/errors/aggregate_test.go`)
  НЕ трогается и остаётся ЗЕЛЁНЫМ — доки теперь его не противоречат.
- ИНВАРИАНТ: `печать(тип(5))` по-прежнему даёт reserved-word ошибку («тип —
  зарезервированное слово…»); регистрация/реализация `тип` НЕ удаляются; функция НЕ активируется.
- ИНВАРИАНТ: код выравнивается доками (не наоборот), кроме версии Go (код — истина,
  берётся из `go.mod`, порог не понижается).

## Критерий «контракт выполнен»

Grep A1–A4 = 0 совпадений; каждое появившееся утверждение подтверждено файл:строкой
кода; `aggregate_test.go` зелёный; `печать(тип(5))` остаётся reserved-word ошибкой;
`онбординг.ladix` согласован с `examples/MANIFEST.md`.
