# Контракт: CLI 006 (`run --db` / `complete` / `tasks`)

**Фаза**: 1 (design) | **Якорь**: `docs/engine-model.md §EN-6` | **Решения**: Q2, Q3, D-20, D-22 |
**FR**: FR-029…FR-034

> Канон usage-строки, команд, флагов и exit-кодов фиксируется §EN-6; этот контракт переносит их
> **дословно** + конвейеры. При расхождении побеждает §EN-6. Тексты CLI-ошибок — [diagnostics.md](./diagnostics.md);
> stdout-форматы — [stdout.md](./stdout.md).

## Usage-строка (дословно §EN-6; `main.go:31`)

```
использование: ladix run [--max-depth N] [--db путь] <файл> | ladix metric [--max-depth N] <файл> <имя> | ladix complete [--db путь] [--max-depth N] <файл> <task-id> | ladix tasks [--db путь] [исполнитель]
```

Разбор флагов — **ручной**, стиль `runMain` (`--флаг значение` и `--флаг=значение`):
- неизвестный флаг → `ladix: неизвестный флаг <а>`, exit 2.
- `--db` без значения → `ladix: флаг --db требует значение`, exit 2 (стиль `--max-depth`).
- Существующие CLI-тексты (`не удалось прочитать файл`, `неверное значение --max-depth`,
  `лишний аргумент`, usage) — **без изменений** (FR-032).

**Сборка движка** (общая для `run`/`complete`/`metric`):

```
interp := eval.NewInterpreter(stdout, maxDepth, eval.SystemClock{})
st     := MemoryStore либо SQLiteStore
eng    := engine.NewEngine(st, interp, stdout)
interp.SetProcessRuntime(eng)
```

У `metric` Store — **всегда** `MemoryStore` (как `run` без `--db`, флага `--db` у неё нет): формула
метрики может через функцию дёрнуть `запустить процесс`/process-builtin (семпроход это разрешает) — без
движка она упёрлась бы в nil-runtime (§EN-8.A); защитная ошибка в CLI 006 **недостижима** (FR-023).

## `ladix run <file.ladix> [--db путь] [--max-depth N]` (FR-029)

1. Чтение файла, лексер → парсер (накопленные ошибки → stderr, exit 1) — как сейчас (`runFile`,
   `main.go:140-161`).
2. **Store:** без `--db` — `MemoryStore` (эфемерно, как сейчас); с `--db` — `SQLiteStore` (Q2).
   Открытие/инициализация не удались → `ladix: не удалось открыть хранилище '<путь>': <причина>`, exit 2.
3. Под `guard`: `interp.Run(prog)` — top-level сверху вниз; `запустить процесс` синхронно доводит инстанс
   до ожидания/терминала (§EN-3). Ошибка → канон §13 stderr, exit 1 (включая провал инстанса, D-14).
4. **Сводка висящих задач:** `st.ListPendingTasks("")`; если непусто — заголовок (строка 5 §EN-7) +
   строки задач (строка 6) в stdout. **Только при N ≥ 1** (где N — число открытых). Exit 0 (висящие
   задачи — не ошибка).
5. **Повторный `run --db` того же файла создаёт НОВЫЕ инстансы** (Q2): id-счётчик персистентен (D-10),
   старые «ожидающие» инстансы остаются в БД и видны в `tasks`. Норма v1.

## `ladix complete <file.ladix> <task-id> [--db путь] [--max-depth N]` (Q3; FR-030)

Ровно **два** позиционных аргумента (файл, task-id); меньше/больше → usage, exit 2.

1. **Компиляция файла:** лексер → парсер → `Analyze` обязаны пройти **чисто**; ошибки → штатный барьер
   (stderr, exit 1). Семантика 005 гарантирует валидность определений процессов. **`interp.Run` НЕ
   вызывается** (повторное исполнение top-level создавало бы новые инстансы на каждый `complete`):
   `GlobalEnv` при `complete` содержит только предопределённые периоды (top-level `пусть` определяет
   только `Run`, `interpreter.go:80-99`) — шаг, читающий top-level `пусть`-глобаль, даёт
   `'<имя>' не объявлено` → инстанс `провален`, exit 1 (граница §EN-10); источники/метрики работают
   (ленивая загрузка, `recordCache`).
2. **Store:** `SQLiteStore`, путь из `--db`, **дефолт `ladix.db`** (README:195). Не открылось → exit 2.
   (`MemoryStore` для `complete` бессмыслен — общего состояния нет.)
3. `eng.Complete(taskID)` — гарды и порядок строго §EN-3 / [engine.md](./engine.md): задача → инстанс →
   **дрейф-гарды Q3** → догон D-4 → гарды D-8 → `MarkTaskCompleted` → печать → продвижение. Нарушение
   гардов → §EN-8.B, exit 2, **инстанс не тронут**. Runtime-ошибка продвижения → инстанс `провален` +
   канон §13, exit 1 (D-14).
4. Успех (включая догон D-4) → stdout строки 7–10 §EN-7, exit 0. **Печатает строки 7–10 сам
   `engine.Complete`** (D-22); CLI по `CompleteResult` ничего не печатает.

## `ladix tasks [исполнитель] [--db путь]` (FR-031)

Файл **НЕ принимает** (Q3): всё из БД. Один опциональный позиционный — фильтр-исполнитель (без него —
все открытые).

1. **Store:** `SQLiteStore`, дефолт `ladix.db`. Движок/интерпретатор **не строятся** (ни Engine, ни файла
   не нужно).
2. `st.ListPendingTasks(фильтр)` → по строке `engine.FormatTaskLine(t, engine.SystemClock{}.Now())` на
   задачу (§EN-7, строка 6); пусто → `открытых задач нет` (строка 11). Exit 0 в обоих случаях.
3. «Просрочена» считает `engine.Overdue` (D-22); `now` — от `engine.SystemClock{}` (инвариант D-2).

## Граница `--db` (FR-034)

Несуществующий путь по валидному каталогу создаёт пустую БД со схемой (`CREATE TABLE IF NOT EXISTS`);
для `complete` это означает `ladix: задача '<id>' не найдена`, для `tasks` — `открытых задач нет`.
Отдельной диагностики «базы нет» в v1 нет. Неоткрываемый путь (нет каталога) →
`ladix: не удалось открыть хранилище '<путь>': <причина>`, exit 2.

## Коды возврата (без изменений канона README; §EN-6, FR-033)

| Код | Условие |
|---|---|
| **0** | успех: `run` (включая висящие задачи), `complete` (включая гард-догон D-4), `tasks` (включая пустой список) |
| **1** | ошибка программы Ladix: компиляция (лекс/синт/семантика), рантайм, **провал инстанса** (D-14); внутренняя паника (`guard`) |
| **2** | ошибка использования/окружения: аргументы (usage), файл, хранилище (открытие/сбой), гарды `complete` (§EN-8.B), дрейф Q3, неизвестный флаг, `--db` без значения |

## Приёмка CLI (§EN-9; FR-042)

- **Сценарий А** (`run`, MemoryStore): exit 0, байт-точный stdout 5 строк (CLI-тесты маскируют только
  `<время>` дедлайнов — id детерминированы).
- **Сценарий Б** (мост SQLite, 6 команд на свежей БД): `run --db` → `tasks` → `tasks Петров` →
  `complete t-000001` → `complete t-000002` → повторный `run --db` (id `p-000002`/`t-000003`); каждый
  шаг — ожидаемый stdout + exit 0.
- **Негативы** (6, после шага 5): несуществующая задача (2), повторная при `выполнен` (2), файл без
  процесса (2, `examples/hello.ladix`), дрейф шага (2, testdata `онбординг-дрейф.ladix`), неоткрываемый
  путь БД (2), парс-ошибка (1, testdata `сломанный.ladix` — НЕ `examples/ошибка.ladix`, та падает в
  рантайме, а `complete` файл не исполняет).
- **Точечные приёмки слоя** (run): печать `2дн`; сравнения; вне диапазона; process-builtins;
  `вчера()` deferred; тип-гард атрибута; провал тела.

Негативы опираются на **testdata-фикстуры** (FR-042): файл-дрейф (процесс есть, шаг переименован) и файл
с **парс**-ошибкой. `examples/MANIFEST.md` синхронизируется (строка онбординга: код 0, golden сценария А,
маска только `<время>`).

## Сопровождение (FR-040/FR-041/FR-043)

- `.gitignore` пополняется `ladix.db` (GAP-9, FR-040).
- `modernc.org/sqlite` (go.mod + go.sum) — **отдельным коммитом** (первая внешняя зависимость, FR-041).
- Все тесты 001–005 остаются зелёными; golden-вывод 004 не меняется ни на байт (D-2, FR-043).
