# Specification Quality Checklist: Движок исполнения процессов

**Purpose**: Validate specification completeness and quality before proceeding to planning
**Created**: 2026-06-11
**Feature**: [spec.md](../spec.md)

## Content Quality

- [x] Нет деталей реализации уровня кода (имена Go-пакетов/функций/сигнатур недопустимы; поверхность CLI/stdout/диагностик допустима) — 4 минорные утечки устранены фикс-проходом, см. Notes
- [x] Фокус на ценности и наблюдаемом поведении (человек-в-цикле как ценность Q1; все требования сформулированы через наблюдаемые stdout/stderr/exit-коды/состояние хранилища)
- [x] Все обязательные секции шаблона `.specify/templates/spec-template.md` заполнены, порядок сохранён (заголовок → User Scenarios & Testing → Edge Cases → Requirements → Key Entities → Success Criteria → Assumptions; доп. секция Out of Scope — прецедент 005)

## Requirement Completeness

- [x] Нет неразрешённых маркеров [NEEDS CLARIFICATION] (grep чист; решения Q1–Q3 и D-1…D-22 закрыты в §EN-1 и в спеке не переоткрываются)
- [x] Требования тестируемы и однозначны (FR-001…FR-043 с MUST/MUST NOT; байт-точные тексты, exit-коды, форматы id, порядок гардов FR-015 = §EN-3)
- [x] Критерии успеха измеримы (SC-001…SC-009: байт-точные golden сценариев А/Б, исчерпывающие счётчики реестров — 11 stdout-форматов, 9+10 текстов диагностик, 28+7=35 builtins, grep-проверяемый инвариант D-2)
- [x] Сценарии приёмки определены в форме Given/When/Then (US1: 5, US2: 6, US3: 9, US4: 7 — все сверены с §EN-9 без дрейфа)
- [x] Edge cases есть, негативы §EN-9 покрыты (все 6 CLI-негативов в US3 + 2 фабрикованных кейса гардов D-8 уровня движка + 13 пограничных случаев в Edge Cases: гард-догон, дрейф-гарды, top-level-глобали при complete, `--db` на несуществующий файл, at-least-once и др.)
- [x] Scope ограничен (Out of Scope зеркалит §EN-0/§EN-10: триггеры/демон/EM-17 → 007, рестарт-скан → 007, арифметика Длительности и 7 дата-builtins — deferred, ОшибкаПроцесса не вводится)
- [x] Зависимости и допущения указаны (Assumptions: вывод из якоря engine-model.md, закрытые решения, готовность фронтенда 005, `modernc.org/sqlite` как первая внешняя зависимость, онбординг-пример как есть, граница эксплуатации v1)

## Feature Readiness

- [x] У требований есть критерии приёмки (каждый блок FR трассируется к SC: хранилище → SC-001/SC-002; движок/гарды → SC-002/SC-003/SC-004; активация → SC-004/SC-008; CLI → SC-001…SC-003; stdout/диагностики → SC-005/SC-006; регрессы → SC-007/SC-009)
- [x] Сценарии покрывают первичные потоки (US1 — запуск до точки ожидания, сценарий А; US2 — персистентная цепочка run --db → tasks → complete, сценарий Б; US3 — гарды целостности; US4 — активированный слой языка)

## Notes

Вердикт: спека высокого качества, полна и трассируема к якорю `docs/engine-model.md` (§EN-0…§EN-10); все находки валидаторов (10 минорных) применены фикс-проходом 2026-06-11 — спека готова к `/speckit.plan`. Маркеров [NEEDS CLARIFICATION] в спеке нет, вопросов архитектору не осталось.

Применено фикс-проходом (все 10 находок; находки по FR-003 совмещены в одну правку):

1. **US3, сценарий 4** — Go-имя поля `CurrentStep` заменено на «текущий шаг инстанса».
2. **FR-035** — Go-layout `2006-01-02 15:04` заменён на `ГГГГ-ММ-ДД ЧЧ:ММ` локальной зоны со ссылкой на байт-точный канон §EN-7.
3. **FR-003/FR-004** — слово «сентинел» убрано (механизм — уровень плана): различимость отказов сформулирована через наблюдаемое поведение и трансляцию в тексты §EN-8; заодно уточнена механика догона (находка 8): догон детектируется по статусу задачи `завершена` до любых мутаций (§EN-3), различимость повторного завершения маршрутизирует в ветку догона только проигравшего гонку D-12.
4. **FR-019** — «callback-интерфейс, объявленный на стороне интерпретатора» заменён на «граница закрыта решением D-1 и не переоткрывается»; механизм — уровень плана реализации.
5. **FR-009** — дополнено по §EN-3: финальное завершение задачи переводит инстанс `ожидает` → `выполнен` **напрямую** (без промежуточного персиста `выполняется`); через `выполняется` — только при наличии следующего шага.
6. **FR-001 ↔ Out of Scope** — разнобой «007 vs v2» по листингам инстансов устранён: аддитивность 007 — только за триггерными операциями (D-3); листинги — вне контракта v006, срок носителя якорь не назначает (D-4); «→ v2» оставлено только за версионированием определений.
7. **Out of Scope, первый буллет** — «состояние перехода» исправлено на «состояние триггеров (`TriggerState`)» (§EN-0/§EN-10).
8. — совмещена с правкой 3 (FR-003, механика догона).
9. **FR-020** — опечатка «непersистируемые» → «неперсистируемые».
10. **Out of Scope, буллет D-11/D-12** — добавлен явный отказ от транзакционного комбо-метода хранилища «завершить + продвинуть» (§EN-2): корректность окна обеспечивает идемпотентный гард-догон D-4.

Принято по прецеденту проекта (не находки):

- Ссылки §EN-N/D-N/EM-N/§PM-N — норма якорной трассировки (прецедент 005: §PM-N/D-N).
- `modernc.org/sqlite` в FR-001/FR-041/Assumptions — решение уровня scope (первая внешняя зависимость проекта, §EN-0), а не деталь реализации.
- SC-007 (`go build`/`go vet`/`gofmt`/`go test`) и SC-009 (grep-инвариант D-2) технологичны, но дословно повторяют принятый паттерн SC-007 спеки 005.
- Имена сущностей ProcessInstance/Task/Store в Key Entities — уровень доменной модели (прецедент 005: ProcessDecl/StepDecl/RunProcessExpr).
- FR-005 (WAL/busy-timeout) и FR-006 (type-tagged JSON/RFC3339) — контракт персистентного файла и конкурентности из закрытых решений EM-7/D-5/D-6/D-21; наблюдаемы через round-trip и параллельный доступ процессов ОС.
- Сверка с якорем: счётчики SC-006 (9 текстов §EN-8.A, 10 текстов §EN-8.B) подтверждены пересчётом; stdout сценариев А/Б и негативы US3 совпадают с §EN-9 дословно; порядок гардов FR-015 = §EN-3; дедлайн в golden (2026-06-03 00:00 = 2026-05-31 + 3дн) корректен.
