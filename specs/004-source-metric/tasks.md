---
description: "Список задач реализации: Декларативный слой v1 — источники и метрики (фича 004)"
---

# Tasks: Декларативный слой v1 — источники и метрики

**Input**: Design documents from `specs/004-source-metric/`
**Prerequisites**: [plan.md](./plan.md) ✅ (Guardrails 1–17, Phasing A–G, Project Structure), [spec.md](./spec.md) ✅ (FR-001..039, SC-001..006, US1–US4), [data-model.md](./data-model.md) ✅, contracts/ ✅ (clock.md, builtins-date.md, metric-engine.md, cli-metric.md)
**Якорь поведения**: `docs/source-metric-model.md` §SM-0…§SM-12 (golden §SM-10, тексты §SM-9 — дословно; при расхождении побеждает он).

**Tests**: ВКЛЮЧЕНЫ обязательно — конституция VI / FR (tests-first). Табличные `*_test.go` пишутся **до** реализации каждого слоя и должны падать до кода; golden-прогон §SM-10 (12 строк) + exact-match §SM-9 (7 краевых) — на гейтах фаз E/G. Время — через `FixedClock{Дата{2026,5,31}}`; вывод — через инжектированный `out` (`bytes.Buffer`).

**Organization**: задачи сгруппированы по фазам A–G плана (фаза A — фундамент; B–F — по приоритетам US1-P1/US2-P1/US3-P2/US4-P3; G — polish/доки-синк). Все пути относительны корня Go-модуля **`src/`** (команды `go` запускаются из `cd src`). Существующие пакеты правятся **аддитивно**; новых пакетов нет (движок метрики — файлы внутри `internal/eval`, R2/§SM-12).

## Format: `[ID] [P?] [Story] Описание`

- **[P]**: можно выполнять параллельно (разные файлы, нет зависимостей от незавершённых задач)
- **[Story]**: к какой User Story относится задача (US1-P1 / US2-P1 / US3-P2 / US4-P3); фазы Foundational/Polish — без метки Story
- Каждая задача указывает точный путь к файлу и привязку: **Guardrail N** (плана) и/или **FR-NNN** и/или **§SM-раздел**

## Соответствие фаз, историй и гейтов (из plan.md «Phasing»)

| Фаза | История | Приоритет | Гейт |
|---|---|---|---|
| A. Фундамент (типы + repr + clock) | — | блокирует всё | `Дата.Compare`, repr R1 (`2175.0`/`1000000.0`/`1000000.5`), `FixedClock` детерминизм зелёные; golden 003 не сломан; **все** вызывающие `NewInterpreter` обновлены (компиляция) |
| B. US1 ч.1 (AST + парсер + регресс) | US1 | P1 🎯 | метрик-онли фикстура парсится с 0 ошибок (SC-004); дубликат/неизвестный атрибут → exact-match §SM-9.A; parse-тесты 002 зелёные; `выручка.ladix → 'процесс'` |
| C. US3 (встроенные дата/сегодня) | US3 | P2 | `дата`/`сегодня` активны; счётчики **25/10/35**; `дата("…")`/`дата(пусто)`/`дата(×3)`/`сегодня()` зелёные; невалид → §SM-9.C |
| D. US1 ч.2 (семпроход Analyze) | US1 | P1 | период-без-по_дате / неизвестный источник / отсутствие обязательного → exact-match §SM-9.A |
| E. US2/US4 (движок метрики + загрузчик) | US2, US4 | P1, P3 | golden §SM-10 12 строк байт-в-байт (SC-001); 7 краевых exact-match (SC-002); имя источника как значение → §SM-9.B |
| F. US2 (CLI metric) | US2 | P1 | `ladix metric <фикстура> выручка_месяца`→`2000000`/код 0; неизвестное имя→stderr/код 1; нет аргументов→код 2 (SC-003) |
| G. Polish (golden + ошибки + доки-синк) | — | финал | SC-001…SC-006 зелёные; `go vet`/`gofmt`/`-race`; регрессы 001/002/003 зелёные |

> **Зависимости фаз** (plan.md): A блокирует всё. B зависит от A (узлы для парсера). C зависит от A (clock для `сегодня`, `Дата` для `дата`). D зависит от B (узлы для семпрохода). E зависит от A+C+D (типы, `дата`, зарегистрированные декларации). F зависит от E (движок). G — последняя.

---

## Phase A: Фундамент — типы + repr + clock (Foundational, блокирует всё)

**Purpose**: довести конструируемые типы `value` (`Дата.Compare`, 5 `Период`, формат `Дробное` R1), завести пакетный `Clock`, расширить `NewInterpreter` третьим параметром и **обновить ВСЕХ вызывающих** (иначе модуль не компилируется). Соответствует фазе A plan.md.

**⚠️ CRITICAL**: ни одна задача фаз B–G не начинается до завершения фазы A. R4-миграция (T006) ломает компиляцию всего модуля до обновления вызывающих — выполнить целиком.

### `internal/value` — доводка конструируемых типов (§SM-5, R1)

- [ ] T001 [P] **Тест+импл `Дата.Compare`** в `src/internal/value/equal.go` (+тест `src/internal/value/equal_test.go`): добавить хелпер `cmpInt(a, b int) int` (по образцу `cmpInt64`); ветвь `case Дата` с вложенным `case Дата` в `value.Compare` — лексикографически `(Year, Month, Day)`, `true`; `Дата`↔не-`Дата` → `0, false`; `Период`↔что-либо остаётся `0, false`. При необходимости добить `Equal`-ветви `Дата`/`Период` параллельно. Тест табличный: `Дата{2026,5,3}<Дата{2026,5,17}`, равенство, несравнимость `Дата↔Целое` (→ `0,false`). *(Guardrail 5, FR-013, §SM-5 §2.1)*
- [ ] T002 [P] **Тест+импл R1 `formatFloat`** в `src/internal/value/repr.go` (+кейсы в `src/internal/value/repr_test.go`): фиксированная запись (без экспоненты) для `|f|∈[1e-4,1e16)`, integer-looking → `+".0"`; sci `g` для очень больших/малых. Инвариант `repr_test.go:16` (`2175→"2175.0"`) **сохранить**; добавить кейсы `1000000.0→"1000000.0"`, `1000000.5→"1000000.5"`, проверить `1e20→"1e+20"`, `3.4→"3.4"`. *(Guardrail 6, R1, §SM-5 §2.3)*
- [ ] T003 [P] **5 предопределённых `Период`-имён** — источник истины в `src/internal/value/periods.go` (+тест при необходимости): экспортированный срез/набор `{ежедневно, еженедельно, ежемесячно, ежеквартально, ежегодно}` (или конструкторы `value.Период{Name:…}`) для регистрации интерпретатором. Только stdlib; `value` остаётся листовым. *(Guardrail 5, FR-014, §SM-5 §2.2)*

### `internal/eval` — Clock-инъекция (§SM-7, R4)

- [ ] T004 [P] **Тест+импл `Clock`** в `src/internal/eval/clock.go` (+тест `src/internal/eval/clock_test.go`): интерфейс `Clock interface{ Now() value.Дата }`; `SystemClock{}` (`time.Now()` в `time.Local`, усечён до Y/M/D — **единственный** легальный `time.Now()` в цепочке); `FixedClock{D value.Дата}` (`Now()=D`). Тест: `FixedClock{Дата{2026,5,31}}.Now()` детерминирован; усечение `SystemClock`. *(Guardrail 8, FR-019/020, §SM-7, контракт clock.md CK-1/CK-2)*
- [ ] T005 **Расширить `Interpreter` + `NewInterpreter`** в `src/internal/eval/interpreter.go`: добавить поле `clock Clock` и реестры декларативного слоя `sources map[string]*ast.SourceDecl` / `metrics map[string]*ast.MetricDecl` / `recordCache map[string][]value.Запись` / `metricStack []string`; `NewInterpreter(out io.Writer, maxDepth int, clock Clock)` (3-й параметр); при инициализации зарегистрировать 5 `Период` (T003) в `global` через `global.Define(...)` (read-only иденты). Тест каркаса: `i.clock` доступен, `ежемесячно` резолвится в `global`. *(depends: T003, T004; Guardrail 5/8, §SM-5 §2.2, §SM-7, data-model §3)*
- [ ] T006 **R4-миграция: обновить ВСЕХ вызывающих `NewInterpreter`** (3-й параметр `clock`) — **отдельная критичная задача**: `src/cmd/ladix/main.go` (`SystemClock{}`); **все** eval-тесты, конструирующие `Interpreter` напрямую (`src/internal/eval/*_test.go`, `src/cmd/ladix/main_test.go`) — `FixedClock{…}` (дата нерелевантна там, где нет `сегодня()`/метрик). Гейт компиляции: `cd src && go build ./... && go vet ./...` зелёные. *(depends: T005; Guardrail 8, R4, §SM-7, контракт clock.md CK-5)*

**Checkpoint (Гейт фазы A)**: `cd src && go build ./...`/`go vet ./...` зелёные; `Дата.Compare` (порядок/несравнимость), repr R1 (`2175.0` инвариант + `1000000.0`/`1000000.5`/`1e+20`), `FixedClock` детерминизм — зелёные; существующий golden 003 **не сломан** (особенно `функция.ladix → 2175.0`).

---

## Phase B: US1 ч.1 — AST + парсер деклараций + регресс (Priority: P1) 🎯 MVP

**Goal**: 2 новых AST-узла + парсер деклараций `источник`/`метрика`; снятие запрета `KW_SOURCE`/`KW_METRIC`; структурная диагностика §SM-9.A; регресс `выручка.ladix → 'процесс'`.

**Independent Test**: метрик-онли фикстуру (§SM-10) подать на разбор → ноль ошибок; варианты с дубликатом/неизвестным атрибутом / не-IDENT в `источник:` → exact-match §SM-9.A с позицией.

### AST-узлы (§SM-2, data-model §1)

- [ ] T007 [US1-P1] **Тест+импл AST-узлов** в `src/internal/ast/decl.go` (+тест `src/internal/ast/decl_test.go`): `SourceDecl{declBase; Name Ident; File StringLit; FilePos Position}` (`Pos()`=токен `источник`); `MetricDecl{declBase; Name Ident; Source Ident; Where/Aggregate/Period/ByDate Expression; Attrs MetricAttrPos}` (`Pos()`=токен `метрика`); вспом. `MetricAttrPos{SourcePos, WherePos, AggregatePos, PeriodPos, ByDatePos Position}` (не `Node`); конструкторы `NewSourceDecl`/`NewMetricDecl` (дословно §SM-2, возвращают указатель). Тест `TestSourceDeclPos`/`TestMetricDeclPos`: `Pos()` через `!=`, поля прямым доступом, компайл-тайм `var _ Decl`/`var _ TopLevelItem`. *(Guardrail 2, FR-001/002, §SM-2, data-model §1)*

### Парсер деклараций (ручной recursive descent, конституция II; §SM-3)

- [ ] T008 [US1-P1] **Снять запрет + ветви диспетчера** в `src/internal/parser/parse_stmt.go`: убрать `KW_SOURCE`/`KW_METRIC` из `isUnexpectedTopLevel` (`:30-38`); добавить ветви `parseSourceDecl`/`parseMetricDecl` в `parseTopLevelItem` **до** проверки `isUnexpectedTopLevel`. `KW_PROCESS`/`KW_WHEN`/`KW_VALUE`/`LBRACE`/`RBRACE` **остаются** отвергаемыми. *(depends: T007; Guardrail 1, FR-006/007, §SM-3)*
- [ ] T009 [US1-P1] **`parseSourceDecl`/`parseSourceRef`** в `src/internal/parser/parse_decl.go` (+позитивные тесты `src/internal/parser/parse_decl_test.go`): разобрать `источник <имя>:` + единственный атрибут `файл: "<путь>"` (`StringLit`); `parseSourceRef` (для `источник:` метрики) читает ровно один `IDENT`, иначе → §SM-9.A `«ожидается имя источника»` (D-1). Пустой блок — переиспользовать `msgEmptyBlock`. Тест: позитивный разбор источника. *(depends: T008; Guardrail 1/3, FR-001/003, §SM-3, §SM-9.A)*
- [ ] T010 [US1-P1] **`parseMetricDecl`/`parseAttrBlock`** в `src/internal/parser/parse_decl.go` (+позитивные тесты `parse_decl_test.go`): разобрать `метрика <имя>:` + блок атрибутов из фикс-набора `{источник,где,агрегат,период,по_дате}`; `источник:` → `parseSourceRef` (T009); `где/агрегат/период/по_дате` → `parseExpression` (приоритеты 002, FR-004); заполнить `MetricAttrPos` позицией ключевого слова каждого присутствующего атрибута. Обязательность/связку парсер НЕ проверяет (D-4 — семпроход). Тест: позитивный разбор полной метрики. *(depends: T008, T009; Guardrail 2/3, FR-002/004, §SM-2/§SM-3)*
- [ ] T011 [US1-P1] **Структурная диагностика §SM-9.A (D-3)** в `src/internal/parser/parse_decl.go` + эталоны в `src/internal/parser/errors.go` (+exact-match тесты в `parse_decl_test.go`): имя атрибута ∉ фикс-набор → `ParseError «неизвестный атрибут '<имя>'»` (поз. имени); повтор любого атрибута → `«атрибут '<имя>' уже задан»` (поз. повтора). Тексты байт-точно из §SM-9.A. Тест exact-match на оба + позиция. *(depends: T010; Guardrail 3/16, FR-005, §SM-3, §SM-9.A)*

### Регресс + позитив-фикстура (§SM-10, FR-038)

- [ ] T012 [US1-P1] **РЕГРЕСС `выручка.ladix → 'процесс'`** в `src/internal/parser/examples_test.go`: `TestDeclarativeExamplesUnexpected` — кейс `выручка.ladix` обновить на ожидание `«неожиданный токен 'процесс'»` (источник/метрика теперь парсятся, падение позже — на `процесс`); `онбординг.ladix` — без изменений. *(depends: T010; Guardrail 17, FR-038, §SM-3)*
- [ ] T013 [US1-P1] **Метрик-онли фикстура (позитив parse-тест)**: положить фикстуру §SM-10 (источник `продажи` + метрика `выручка_месяца`, БЕЗ `процесс`/`когда`) как тест-фикстуру в `src/internal/eval/testdata/` (НЕ в `examples/` — вывод дата-зависим); parse-тест в `src/internal/parser/parse_decl_test.go` — фикстура разбирается с **нулём ошибок** (SC-004). *(depends: T010; Guardrail 17, FR-038, §SM-10, SC-004)*

**Checkpoint (Гейт фазы B)**: метрик-онли фикстура парсится с 0 ошибок (SC-004); дубликат/неизвестный атрибут / не-IDENT в `источник:` → exact-match §SM-9.A с позицией; `выручка.ladix → 'процесс'`; **все** parse-тесты 002 зелёные.

---

## Phase C: US3 — встроенные `дата`/`сегодня` (Priority: P2)

**Goal**: активировать из deferred ровно две встроенные; `дата` (overloaded) + `сегодня` (fixed N=0, читает `i.clock`); обновить счётчики реестра. Активирует ось времени для US2.

**Independent Test**: `дата("2026-05-03")`→дата (`2026-05-03`); `дата(пусто)`→`пусто`; `дата(2026,5,3)`→та же дата; `сегодня()` при `FixedClock{2026,5,31}`→`2026-05-31`; `дата("2026-13-40")`/`дата(2026,13,1)`→exact-match §SM-9.C.

- [ ] T014 [US3-P2] **Активация + счётчики реестра** в `src/internal/eval/builtins.go` (+обновить `src/internal/eval/builtins_test.go`): убрать `"дата"`/`"сегодня"` из `deferredNames` (`:48`); зарегистрировать `add(fixed("сегодня", 0, builtinSegodnya))` + `add(overloaded("дата", builtinData))`. **Обновить счётчики**: `TestBuiltinRegistryClosed` (активных **23→25**, deferred **12→10**, всего **35**), `TestBuiltinDeferredAll` (итерация по 10 deferred-именам). *(Guardrail 7, FR-015/016/018, §SM-6)*
- [ ] T015 [US3-P2] **Тест+импл `builtinSegodnya`/`builtinData`** в `src/internal/eval/builtins_date.go` (+тесты `src/internal/eval/builtins_date_test.go`): `builtinSegodnya` (`ArityFixed N=0`) → `i.clock.Now()` (НЕ `time.Now()`), фиксирована на запуск; `builtinData` (`ArityOverloaded`) — `дата(Строка)` парсит ровно `YYYY-MM-DD` (валидный календарь, високосный фев, `T…`→невалидно); `дата(Пусто)`→`Пусто`; `дата(Целое×3)`→сборка с валидацией. Табличные тесты позитива: `дата("2026-05-03")`, `дата(пусто)`, `дата(2026,5,3)`, `сегодня()`=`FixedClock`. *(depends: T014, фаза A; Guardrail 7, FR-015/016/017, §SM-6, контракт builtins-date.md)*
- [ ] T016 [US3-P2] **Exact-match ошибки `дата` §SM-9.C** в `src/internal/eval/builtins_date_test.go`: `дата("2026-13-40")`/`дата("2026-02-29")`/`дата("2026-05-03T…")` → `«дата: «<значение>» не является датой»`; `дата(2026,13,1)` → `«дата: некорректные год/месяц/день»`; `дата(истина)` (1 арг неверного типа) → `«дата: ожидается Строка или Пусто, получено Булево»`. Позиция = `CallExpr.Pos()`; тексты байт-точно. *(depends: T015; Guardrail 7/16, FR-017, §SM-9.C)*

**Checkpoint (Гейт фазы C)**: `дата`/`сегодня` активны; счётчики `25/10/35`; позитив `дата(…)`/`сегодня()` зелёные; невалидные даты → exact-match §SM-9.C.

---

## Phase D: US1 ч.2 — семпроход `Analyze` (Priority: P1)

**Goal**: объединить Шаг 1 `Analyze` в один упорядоченный проход `FunctionDecl`+`SourceDecl`+`MetricDecl`; повтор имени (R5); Шаг 1b — обязательность `источник`/`агрегат`, связка `период↔по_дате`, резолв `Source`→источник.

**Independent Test**: период-без-по_дате / по_дате-без-период / неизвестный источник / отсутствие обязательного / занятое имя → exact-match §SM-9.A с правильной позицией.

- [ ] T017 [US1-P1] **Шаг 1 — объединённый проход + регистрация** в `src/internal/eval/analyze.go` (+exact-match тесты `src/internal/eval/analyze_test.go`): объединить регистрацию в **один упорядоченный** проход по `FunctionDecl`+`SourceDecl`+`MetricDecl` (общее глобальное пространство), заполняя `i.sources`/`i.metrics`; повтор имени **с участием источника/метрики** → `СемантическаяОшибка «'<имя>' уже объявлено в строке N»` (§SM-9.A); функция↔функция → оставить существующий `«функция '<имя>' уже объявлена в строке N»` (регресс 003, R5). Тест exact-match на обе формы повтора. *(depends: T007, фаза B; Guardrail 4, FR-008, §SM-4, §SM-9.A)*
- [ ] T018 [US1-P1] **Шаг 1b — статическая валидация метрики** в `src/internal/eval/analyze.go` (+exact-match тесты `analyze_test.go`): `источник`/`агрегат` обязательны → `«метрика '<имя>': отсутствует обязательный атрибут 'источник'/'агрегат'»` (поз. `метрика`); `период` ⇔ `по_дате` → `«…: 'период' требует 'по_дате'»` (поз. `Attrs.PeriodPos`) / `«…: 'по_дате' без 'период' не имеет смысла»` (поз. `Attrs.ByDatePos`); `Source` → зарегистрированный источник: не найдено → `«…: источник '<ист>' не объявлен»` (поз. `Attrs.SourcePos`), занято функцией/метрикой → `«…: '<ист>' — не источник»`. НЕ резолвит поля/типы/голое поле/цикл (D-5/D-6/D-8 — eval-time). Тексты байт-точно §SM-9.A. *(depends: T017; Guardrail 4/16, FR-009/010/011/012, §SM-4, §SM-9.A)*

**Checkpoint (Гейт фазы D)**: период-без-по_дате / неизвестный источник / отсутствие обязательного атрибута → exact-match §SM-9.A (SC-002 частично); функция↔функция-повтор сохраняет старый текст (регресс 003).

---

## Phase E: US2/US4 — движок метрики + JSON-загрузчик (Priority: P1/P3)

**Goal**: ленивый JSON-загрузчик (`UseNumber`, маппинг §9.3, ошибки §SM-9.B, схема, кеш); календарные окна по `Период`; конвейер `окно→фильтр→агрегат-проекция` с контекстами D-9; реестр метрик + `evalIdent`-ветви источник/метрика + цикл-детект; пустой результат §10.5; golden §SM-10 + 7 краевых exact-match.

**Independent Test**: golden §SM-10 12 строк байт-в-байт при `FixedClock{2026,5,31}`+`data/sales.json` (SC-001); 7 краевых ошибок exact-match §SM-9 (SC-002); имя источника как значение → §SM-9.B.

### JSON-загрузчик источника (§SM-8.1)

- [ ] T019 [US2-P1] **Тест+импл `loadSource`** в `src/internal/eval/source_loader.go` (+тесты `src/internal/eval/source_loader_test.go`): `i.loadSource(name) ([]value.Запись, error)` — `json.Decoder` + **`UseNumber()`**; верхний уровень — массив объектов; маппинг §9.3: `null→Пусто`, `bool→Булево`, строка→`Строка` (даты НЕ распознаются), `json.Number` без `.`/`e`/`E`→`Целое` (вне int64→ошибка), с `.`/экспонентой→`Дробное`, массив→`Список`, объект→`Запись` (через `value.NewRecord`); путь — относительно cwd (§9.1); **лень + кеш на запуск** (`i.recordCache`). Схема = объединение ключей всех записей, **отсортирована**. Тест: маппинг типов, различение `1200000`/`1200000.0`/`1.2e6`, кеш не перечитывает. *(depends: фаза A; Guardrail 13, FR-021/022/024, §SM-8.1, контракт metric-engine.md ME-5)*
- [ ] T020 [US2-P1] **Exact-match ошибки загрузки §SM-9.B + int64-границы** в `src/internal/eval/source_loader_test.go`: файл не найден → `«источник '<имя>': файл «<путь>» не найден»`; не массив → `«…: ожидался JSON-массив объектов в «<путь>»»`; элемент не объект → `«…: запись N не является объектом»` (N с 1); битый JSON → `«…: некорректный JSON в «<путь>» (<деталь>)»`; целое вне int64 → `«…: запись N, поле '<поле>': целое число вне диапазона»`. Позиция = объявление `источник`; тексты байт-точно. *(depends: T019; Guardrail 13/16, FR-023, §SM-9.B)*

### Календарные окна (§SM-8.2)

- [ ] T021 [P] [US2-P1] **Тест+импл окон по `Период`** в `src/internal/eval/window.go` (+тесты `src/internal/eval/window_test.go`): `D=clock.Now()`; полный период, содержащий `D`, **включительно** обе границы (через `Дата.Compare`): `ежедневно [D,D]`; `еженедельно` ISO пн–вс (`time.Weekday`/`ISOWeek`, Sun=0→ISO Mon=1..Sun=7, окно может пересекать границу месяца); `ежемесячно [1, послед_день]` (високосный фев=29); `ежеквартально` по `M`; `ежегодно [1.1,31.12]`. Арифметика через `time.Date(...)` (служебная Go). Тест: `D=2026-05-31` → `ежемесячно [05-01,05-31]`, `еженедельно [05-25,05-31]` (вс на границе), `ежеквартально [04-01,06-30]`, `ежегодно [01-01,12-31]`, `ежедневно [05-31,05-31]`. *(depends: T001, T004; Guardrail 14, FR-025, §SM-8.2, контракт metric-engine.md ME-6)*

### Движок метрики — конвейер + контексты D-9 (§SM-8)

- [ ] T022 [US2-P1] **Тест+импл `evalMetric` (конвейер + контексты D-9)** в `src/internal/eval/metric_engine.go` (+тесты `src/internal/eval/metric_engine_test.go`): `i.evalMetric(m *ast.MetricDecl) (value.Value, error)`. Контексты (D-9): `период:` — **один раз** в глобальной области → `Период` (иначе `«'период' должно давать Период, получено <тип>»`, §SM-9.B); `где`/`по_дате`/проекции агрегата — **per-record** в scope полей (`Запись` поверх `global`); `источник:` не вычисляется (имя). Конвейер §10.2: загрузка (T019) → окно (T021, `D=clock.Now()`, полный календарный) → фильтр (`по_дате`∈окно И `где`=истина; `по_дате=Пусто`→исключить без ошибки/без `где`) → агрегат-проекция. Тест: метрика, где `период:` ссылается на глобаль, а `где` — на поле записи (риск D-9). *(depends: T019, T021, фаза C, фаза D; Guardrail 10, FR-025/026/027, §SM-8, контракт metric-engine.md ME-1/ME-2/ME-3)*
- [ ] T023 [US4-P3] **Агрегат-проекция (R3) + голое поле/вложенный агрегат** в `src/internal/eval/metric_engine.go` (+тесты `metric_engine_test.go`): для вызовов агрегатных функций (`сумма/количество/среднее/мин/макс`) внутри `агрегат:` вычислить аргумент-выражение НА КАЖДОЙ выжившей записи (scope полей), построить проекцию-`Список` (пропуская записи, где даёт `Пусто`, §10.3), затем звать существующий builtin `Fn` (`ArityFixed N=1`, `requireList`). Голое поле **вне** агрегата → `ОшибкаВыполнения «поле '<имя>' использовано вне агрегатной функции»`; вложенный агрегат (`сумма(среднее(...))`) → `«вложенный агрегат недопустим»`. Тест: проекция, голое поле, вложенный агрегат. *(depends: T022; Guardrail 11, FR-028/029, §SM-8, §SM-9.C, контракт metric-engine.md ME-4)*
- [ ] T024 [US2-P1] **Пустой результат §10.5 (короткое замыкание)** в `src/internal/eval/metric_engine.go` (+тесты `metric_engine_test.go`): нет выживших записей → **короткое замыкание ДО** вызова агрегата (чтобы `«<имя>: список пуст»` НЕ срабатывало в контексте метрики): корневой единичный `сумма`/`количество` → `Целое 0`; иначе (`среднее`/`мин`/`макс`/деривативы) → `Пусто`. Тест: пустое недельное окно → `сумма`=`0`, `среднее`=`пусто`. *(depends: T023; Guardrail 12, FR-030, §SM-8.5)*
- [ ] T025 [US4-P3] **Разрешение голого имени + опечатка поля (схема)** в `src/internal/eval/metric_engine.go` (+тесты `metric_engine_test.go`): порядок резолва голого имени в `где`/`агрегат`/`по_дате` (per-record): global/Период → funcs/builtins → поле схемы (нет в записи → `Пусто` через `Запись.Get`) → иначе `ОшибкаВыполнения «неизвестное поле '<имя>' (известные: <отсортировано>)»`; `где`-не-`Булево` → `ОшибкаТипа «'где' должно давать Булево, получено <тип>»`; `по_дате`-не-`Дата`/`Пусто` → `«'по_дате' должно давать Дата или Пусто, получено <тип>»`. Тест: опечатка поля (отсортированный список), `где: статус`(Строка)→ошибка. *(depends: T022; Guardrail 11, FR-027/029, §SM-8.1, §SM-9.C, data-model §6)*

### Реестр метрик + evalIdent + цикл-детект (R6/D-8, §SM-9.B)

- [ ] T026 [US2-P1] **`evalIdent`-ветви источник/метрика + цикл-детект** в `src/internal/eval/expr.go` (источник/метрика) и `src/internal/eval/metric.go` (реестр/запуск/цикл) (+тесты): в `evalIdent` после промаха `env.Lookup` и ДО ветки «функция/не объявлено»: имя ∈ `i.sources` → `ОшибкаВыполнения «'<имя>' — источник, его нельзя использовать как значение»` (FR-032); имя ∈ `i.metrics` → `i.evalMetric(...)`; цикл (через `i.metricStack`, push/pop в `evalMetric`) → `«цикл зависимостей метрик: <имя> → … → <имя>»` (FR-031, D-8). Тест: имя источника как значение → §SM-9.B; две взаимно-ссылающиеся метрики → цикл. *(depends: T022; Guardrail 9, FR-031/032, §SM-4/§SM-8, §SM-9.B, контракт metric-engine.md ME-7)*

### Golden + краевые exact-match (§SM-10)

- [ ] T027 [US2-P1] **Golden §SM-10 — 12 строк** в `src/internal/eval/metric_engine_test.go`: при `FixedClock{Дата{2026,5,31}}` + `data/sales.json` (3 записи мая 2026) воспроизвести таблицу §SM-10 байт-в-байт: `ежемесячно/ежеквартально/ежегодно`→`2000000`; `еженедельно/ежедневно`→`0`; без периода→`2000000`; без `где`→`2500000`; `количество`→`2`; `среднее`→`1000000.0`; `мин`→`800000`; `макс`→`1200000`; `среднее` на пустом окне→`пусто`. Сверка через `value.String(результат)`. *(depends: T024, T025; Guardrail 16/17, FR-038, §SM-10, SC-001)*
- [ ] T028 [US4-P3] **7 краевых exact-match §SM-9** в `src/internal/eval/metric_engine_test.go`: период-без-по_дате (§SM-9.A); неизвестный источник (§SM-9.A); файл не найден (§SM-9.B); где-не-Булево (§SM-9.C); голое-поле (§SM-9.C); опечатка-поля с отсортированным списком (§SM-9.C: `«неизвестное поле 'статус_опечатка' (известные: дата_заказа, клиент, статус, сумма_заказа)»`); дата-невалидна (§SM-9.C). Тексты байт-точно; позиция проверяется. *(depends: T026, T016, T018; Guardrail 16, FR-039, §SM-10, SC-002)*

**Checkpoint (Гейт фазы E)**: golden §SM-10 12 строк байт-в-байт (SC-001); 7 краевых ошибок exact-match §SM-9 (SC-002); имя источника как значение → §SM-9.B; цикл метрик → §SM-9.B.

---

## Phase F: US2 — CLI `ladix metric` (Priority: P1)

**Goal**: ветвление `realMain` по `args[0]`; `ladix metric <файл> <имя>` → конвейер → печать `value.String`; коды 0/1/2; §SM-9.D; `SystemClock`; `guard`-барьер общий с `run`; `run` без изменений.

**Independent Test**: `ladix metric <фикстура> выручка_месяца`→`2000000`/код 0; неизвестное имя→stderr `«неизвестная метрика '<имя>'»`/код 1; нет двух позиционных→usage/код 2.

- [ ] T029 [US2-P1] **Ветвь `metric` в диспетчере** в `src/cmd/ladix/main.go` (+тесты `src/cmd/ladix/main_test.go`): `realMain` — ветвление по `args[0]` (`run` как есть; `metric`); `ladix metric <файл> <имя> [--max-depth N]`: лексер→парсер→`Analyze`→`i.evalMetric`→`value.String(результат)` в stdout + `\n`; `SystemClock{}` (прод), фиксируется на запуск; `guard`-барьер — общий с `run`; без store/`--db`; `run` работает без изменений. Тест: `ladix metric <фикстура> выручка_месяца`→`2000000`/код 0. *(depends: фаза E; Guardrail 15, FR-033/037, §SM-11, контракт cli-metric.md)*
- [ ] T030 [US2-P1] **Коды выхода + §SM-9.D** в `src/cmd/ladix/main.go` (+тесты `main_test.go`): нет двух позиционных → usage, код **2** (FR-034); неизвестная метрика → `«неизвестная метрика '<имя>'»`, код **1**; имя-не-метрика (переменная/функция/источник) → `«'<имя>' — не метрика»`, код **1** (§SM-9.D); ошибки загрузки/вычисления → stderr, код **1**; Go-паника → `внутренняя ошибка интерпретатора` без stack trace, код **1** (FR-036). Тест: неизвестная метрика→stderr/код 1; нет аргументов→код 2. *(depends: T029; Guardrail 15/16, FR-034/035/036, §SM-9.D, §SM-11)*

**Checkpoint (Гейт фазы F)**: `ladix metric <фикстура> выручка_месяца`→`2000000`/код 0; неизвестное имя→stderr/код 1; нет аргументов→код 2 (SC-003); `run` без изменений (FR-037).

---

## Phase G: Polish — golden + ошибки + доки-синк (финал)

**Purpose**: сквозной прогон golden §SM-10 + exact-match §SM-9; синк трёх doc-файлов; конституционные гейты; зелёные регрессы 001/002/003.

- [ ] T031 [P] **Doc-синк R1 + §SM-5** в `docs/eval-model.md §7` (политика формата `Дробное` — фиксированная запись для бизнес-диапазона) и `docs/source-metric-model.md §SM-5` (снять «repr не трогаем»; зафиксировать repr-кейсы `1000000.0`/`1000000.5`). *(Guardrail 6/17, R1, FR-039, §SM-5)*
- [ ] T032 [P] **Doc-синк stdlib + §8.3/§SM-9** в `docs/stdlib.md` (`дата`/`сегодня` — «доступны»; `длительность`/`в_*`/`вчера`/`завтра` — «v2 (deferred)»; тексты ошибок `дата` дословно §SM-9.C), `docs/eval-model.md §8.3` (перенести строки `дата:` — слой eval), `docs/source-metric-model.md §SM-5/§SM-9` (синк); байт-идентичность с тестами. *(Guardrail 7/16/17, FR-039, §SM-6/§SM-9)*
- [ ] T033 **Сквозная golden+exact-match приёмка** в `src/internal/eval/` и `src/cmd/ladix/`: прогнать golden §SM-10 (12 строк) + 7 exact-match §SM-9 + CLI-golden (`ladix metric`→`2000000`/код 0); сверить, что все тексты ошибок в тестах байт-идентичны §SM-9 (A/B/C/D) и `eval-model §8.3` (строки `дата:`). *(Guardrail 16, FR-039, §SM-9/§SM-10, SC-001/SC-002/SC-003)*
- [ ] T034 **Конституционные гейты + регрессы (SC-005/SC-006)**: `cd src && go build ./...` и `go vet ./...` без замечаний; `gofmt -l .` без диффа; `go test -race ./...` зелёное, включая **все** регресс-тесты 001/002/003 (FR-038); подтвердить, что ни один путь `eval`/движка не зовёт `time.Now()` напрямую (только `SystemClock.Now()`) — SC-006; зафиксировать в финальном прогоне. *(depends: T031, T032, T033; FR-038, SC-005/SC-006)*

**Checkpoint (Гейт фазы G)**: SC-001…SC-006 зелёные; golden §SM-10 + 7 exact-match §SM-9 байт-в-байт; `go vet`/`gofmt`/`-race` чисто; регрессы 001/002/003 зелёные.

---

## Dependencies & Execution Order

### Зависимости фаз

- **Фаза A (Foundational)**: без зависимостей — старт немедленно. **БЛОКИРУЕТ всё** (типы/clock/repr — фундамент; R4-миграция T006 ломает компиляцию до обновления вызывающих).
- **Фаза B (US1 ч.1)**: зависит от A (AST-узлы → парсер).
- **Фаза C (US3)**: зависит от A (`clock` для `сегодня`, `Дата` для `дата`).
- **Фаза D (US1 ч.2)**: зависит от B (узлы для семпрохода).
- **Фаза E (US2/US4)**: зависит от A+C+D (типы, `дата`, зарегистрированные декларации).
- **Фаза F (US2 CLI)**: зависит от E (движок метрики).
- **Фаза G (Polish)**: зависит от завершения A–F.

### Ключевые внутрифазовые зависимости

- T005 зависит от T003 (периоды), T004 (Clock); **T006 (R4-миграция) зависит от T005** — критично, иначе модуль не компилируется.
- T008–T013 зависят от T007 (AST-узлы); T009/T010 — последовательны (parseSourceRef ← parseMetricDecl); T011 — после T010.
- T015 зависит от T014 (реестр) + фазы A (Clock/Дата); T016 — после T015.
- T018 зависит от T017 (Шаг 1 регистрирует реестры для Шага 1b).
- T020 зависит от T019; T021 — от T001/T004 (Дата.Compare + Clock).
- T022 зависит от T019, T021, фаз C+D; T023/T024/T025 — после T022; T026 — после T022.
- T027 зависит от T024, T025; T028 — от T026, T016, T018 (собирает ошибки парсера/семпрохода/движка/дат).
- T029 зависит от фазы E; T030 — от T029.
- T034 зависит от T031, T032, T033 (финальный гейт).

### Within Each Phase

- Табличные/golden/exact-match тесты ПИШУТСЯ В ПАРЕ с импл-задачей и должны падать до кода (tests-first, конституция VI).
- Типы/clock → парсер/семпроход → загрузчик/окна → движок → CLI → polish.
- Фаза завершается своим гейтом до перехода к следующей.

---

## Parallel Opportunities

- **Фаза A**: T001, T002, T003 (разные файлы `equal.go`/`repr.go`/`periods.go`) и T004 (`clock.go`) — параллельно; T005 — после T003/T004; T006 — после T005.
- **Фаза E**: T021 (`window.go`) параллельно с T019/T020 (`source_loader.go`); doc-синк T031/T032 (фаза G, разные доки) — параллельно.
- Внутри фаз все помеченные [P] задачи затрагивают разные файлы.

### Parallel Example: Phase A (фундамент)

```bash
# value-доводка (разные файлы) — параллельно:
Task: "Дата.Compare + cmpInt в internal/value/equal.go"
Task: "R1 formatFloat в internal/value/repr.go"
Task: "5 предопределённых Период в internal/value/periods.go"
# Clock (отдельный файл) — параллельно:
Task: "Clock/SystemClock/FixedClock в internal/eval/clock.go"
```

---

## Implementation Strategy

### MVP First (US1 + US2 = P1)

1. Фаза A (фундамент — КРИТИЧНО, R4-миграция T006 ломает компиляцию до обновления вызывающих).
2. Фаза B (US1 ч.1: AST + парсер) → Фаза C (US3: даты, нужна для оси времени) → Фаза D (US1 ч.2: семпроход).
3. Фаза E (US2/US4: движок) → Фаза F (US2: CLI).
4. **STOP & VALIDATE**: golden §SM-10 (12 строк) + 7 exact-match §SM-9 + `ladix metric`→`2000000`/код 0 (SC-001/SC-002/SC-003).

### Incremental Delivery

1. A → фундамент (типы/clock/repr; компиляция зелёная).
2. + B → декларации парсятся (метрик-онли фикстура, 0 ошибок).
3. + C → `дата`/`сегодня` активны (ось времени).
4. + D → статическая валидация деклараций (exact-match §SM-9.A).
5. + E → метрики считаются (golden §SM-10, краевые §SM-9).
6. + F → `ladix metric` (коды 0/1/2).
7. + G → доки-синк + конституционные гейты + регрессы.

---

## Notes

- **Tests-first обязателен** (конституция VI): табличные/golden/exact-match `*_test.go` пишутся в паре с кодом и падают до реализации; вывод через инжектированный `out` (`bytes.Buffer`), время через `FixedClock{Дата{2026,5,31}}`.
- **Тексты ошибок — ДОСЛОВНО**: новый слой — §SM-9 (A/B/C/D); строки `дата:` (слой eval) — `eval-model §8.3`; при расхождении побеждает якорь `docs/source-metric-model.md`. Payload без завершающей точки; `'…'` — идентификаторы; `«…»` — строковые значения; `N` с 1; колонка в **рунах**.
- **Без новых типов ошибок**: 4 готовых типа `errors` (`ParseError`/`СемантическаяОшибка`/`ОшибкаВыполнения`/`ОшибкаТипа`) покрывают весь новый слой — 004 добавляет только новые `Msg`.
- **Без новых пакетов** (R2/§SM-12): движок/загрузчик/окна/`Clock` — файлы внутри `internal/eval`; `internal/store`/`internal/engine` НЕ заводятся. Граф ацикличен; `value`/`errors`/`ast` — листовые.
- **Закрытый реестр стдлиб**: после активации `дата`/`сегодня` — РОВНО **25 активных + 10 deferred = 35**; счётчики `TestBuiltinRegistryClosed`/`TestBuiltinDeferredAll` править вместе с реестром (T014).
- **R4-миграция (T006)** — третий параметр `clock` у `NewInterpreter` затрагивает `cmd/ladix` и ВСЕ eval-тесты; пропуск → провал компиляции (это и есть страховка).
- **Контексты атрибутов (D-9)** — `период:` раз в глобальной области, `где`/`по_дате`/проекции — per-record в scope полей; перепутать → неверный результат/ложные «неизвестное поле» (тест в T022).
- **Метрик-онли фикстура** — в `testdata/` eval, НЕ в `examples/` (вывод дата-зависим); `выручка.ladix` целиком НЕ парсится (`процесс`) — регресс на `'процесс'` (T012).
- Все `go`-команды из `cd src`; коммит после каждой задачи или логической группы; останов на любом checkpoint для автономной валидации фазы.

---

**Итого: 34 задачи** (T001–T034). По фазам: **A** — 6 (T001–T006), **B** — 7 (T007–T013), **C** — 3 (T014–T016), **D** — 2 (T017–T018), **E** — 10 (T019–T028), **F** — 2 (T029–T030), **G** — 4 (T031–T034).
