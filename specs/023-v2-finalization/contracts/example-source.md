# Контракт: `examples/контроль_плана.ladix` (W1)

**Якорь**: `docs/v2-finalization-model.md §F-1` — «Целевое содержимое» (копи-реди блок). D-1=a:
расширить существующий файл, НЕ плодить новый. Синтаксис — канон; комментарии — на усмотрение импла.

## Целевое содержимое (копи-реди из §F-1)

```ladix
# Контроль плана продаж с эскалацией — золотой сквозной сценарий v2 (DoD §2 хартии).
# Прошивает аналитику (M1) → триггер → автоматизацию (M2) → надёжность (M3):
#   M1: CSV-источник со схемой полей + метрика как скользящее окно «последние 30дн».
#   связь: «когда метрика < план» запускает процесс, передавая снимок как аргумент.
#   M2: человеческий шаг+срок, эскалация дедлайна, payload (--данные), реальный эффект.
#   M3: эффект тела шага доставляется exactly-once через outbox даже на рестарте демона.
# Запуск — docs/quickstart.md §5 (serve/start/complete); данные — data/orders.csv.

# ── M1: аналитика ───────────────────────────────────────────────
источник заказы:
    файл: "data/orders.csv"
    тип: csv
    поля:
        дата_заказа: Дата
        сумма_заказа: Дробное
        статус: Строка

метрика выручка_30д:
    источник: заказы
    где:      статус == "оплачен"
    агрегат:  сумма(сумма_заказа)
    период:   последние 30дн
    по_дате:  дата_заказа

# ── связь: падение выручки ниже плана запускает процесс ─────────
когда метрика выручка_30д < 3_000_000:
    запустить процесс эскалация_плана(значение)

# ── M2: автоматизация (человек-в-цикле + эскалация + payload + эффект) ──
процесс эскалация_плана(факт):
    шаг связаться_с_клиентом:
        исполнитель: "менеджер"
        срок:        3дн
    # Авто-шаги догона (M3 §C-1, split D-C-5): durable-захват payload отдельно
    # от реального эффекта; на рестарте эффект переисполняется, но outbox
    # (exactly-once) глушит повтор — POST в crm РОВНО один раз.
    шаг зафиксировать_итог после связаться_с_клиентом:
        присвоить итог = данные.итог
    шаг уведомить_crm после зафиксировать_итог:
        уведомить crm("итог звонка: " + итог)

# ── эскалация дедлайна (исполняет демон serve; под run — заглушка) ──
когда задача просрочена в эскалация_плана.связаться_с_клиентом:
    уведомить руководитель(факт)
```

## Контрактные требования

- **CR-1**: ВКЛЮЧАЕТ все 7 фасетов §2-цепочки: источник `заказы` (CSV + `поля:`), окно-метрика
  `выручка_30д` (`последние 30дн`), метрика-триггер `когда метрика выручка_30д < 3_000_000` (передаёт
  `значение`), человеческий шаг `связаться_с_клиентом` (исполнитель + срок), эскалация
  `когда задача просрочена`, payload-чтение `данные.итог`, реальный эффект `уведомить crm`.
- **CR-2**: НЕ содержит хардкод-литерального старта: строки `пусть id = запустить процесс
  эскалация_плана(2500000)` и `печать(…)` — УДАЛЕНЫ. Число `2500000` и `печать` в файле отсутствуют.
- **CR-3**: парсится **без единой диагностики** → входит в чистый набор
  `TestExamplesParseCleanSet` (`src/internal/parser/examples_test.go:11`).
- **CR-4**: источник/метрика впаяны **дословно** из `выручка_30д.ladix` (тот же `data/orders.csv`, та
  же окно-метрика) → консистентный снимок `300000.0` под FixedClock.

## Что НЕ трогаем
- `examples/выручка_30д.ladix`, `examples/выручка.ladix` — без изменений (M1-обучающие).
- `data/orders.csv` — без изменений (общая фикстура).
- `examples/MANIFEST.md` — вне границы (архитектор, FR-016).
