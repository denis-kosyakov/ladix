# Contract: каскад зеркал SE-TRIGGER-KIND (B4a)

Источник §AU-10.A. Группа: **B4a-фронтенд**. Единственный сдвиг golden-строки фронтенда в M2 —
многоточечный.

## Изменение

`const msgTriggerKind` (`parser/errors.go:29`):
- ДО: `"метрика, событие или расписание"`
- ПОСЛЕ: `"метрика, событие, расписание или задача"`

Тот же код **SE-TRIGGER-KIND** (счёт SE=14 НЕ меняется — РАСШИРЕНИЕ текста, не новый код).
`задача` теперь валидное продолжение после `когда`.

## Зеркала-замки (МОЯ зона B4a — ОБЯЗАТЕЛЬНО со-обновить, иначе `go test` красный)

| # | Файл:строка | Тип | Со-правка |
|---|-------------|-----|-----------|
| 1 | `parser/errors.go:29` | const | новый текст |
| 2 | `parser/errors.go:26` | коммент | если цитирует текст — обновить |
| 3 | `parser/inventory_test.go:34` | fragment-match | `"метрика, событие, расписание или задача"` |
| 4 | `parser/parse_decl_test.go:1549` | exact `TestTriggerSyntaxDiagnostics` | `ожидалось 'метрика, событие, расписание или задача', получено 'x'` |
| 5 | `parser/parse_decl_test.go:1622` | exact `TestTriggerNegativesExactPos` | `… получено 'X'` (+line/col без изменений) |
| 6 | `parser/parse_decl_test.go:1666` | двухстрочный golden `TestGoldenTriggerSyntaxDiagnostics` | `Ошибка в строке 1, колонка 7:\nожидалось 'метрика, событие, расписание или задача', получено 'X'` |

## Доковые зеркала (СИНКАЕТ АРХИТЕКТОР на M2-гейте — НЕ зона B4)

B4 НЕ редактирует большие доки, но перечисляет:
- `docs/trigger-model.md:432`, `:433`, `:1070`, `:1087`.
- `SPEC §13` / `docs/diagnostics-model.md` — любые цитаты старого текста.

## Позитив (новый вид валиден)

После расширения `когда задача просрочена в P.S:` — НЕ даёт SE-TRIGGER-KIND (распознаётся как
DeadlineTrigger). Только нераспознанный ведущий токен (`когда мусор:`, `когда x > 1:`,
`когда X:`) → SE-TRIGGER-KIND с новым текстом.

## Тесты (B4a-группа)

- `когда мусор:` → `ожидалось 'метрика, событие, расписание или задача', получено 'мусор'`.
- Все 6 зеркал-замков зелёные после со-обновления.
- Счётный замок `parser/inventory_test.go:38` `wantCodes=14` — НЕ меняется (тот же код).
- Инверсия: оставить старый текст в `errors.go:29`, обновить только тесты → зеркало 3/4/6
  красное (текст разошёлся) → доказывает, что обновлять надо ИСТОЧНИК, а не подгонять тесты.
- Инверсия: НЕ обновить зеркало `inventory_test.go:34` после правки `errors.go` → красное.
