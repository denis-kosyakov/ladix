# Contract: семпроход checkTrigger — кейс DeadlineTrigger (B4a)

Источник §AU-6.1.3 / §AU-10.B. Группа: **B4a-фронтенд**.

## Кейс в `checkTrigger` (eval/analyze.go:319)

Добавить в switch `td.Spec.(type)` кейс `*ast.DeadlineTrigger`:

```go
case *ast.DeadlineTrigger:
    // (а) процесс объявлен
    pd, ok := i.processes[spec.Process.Name]
    if !ok {
        return semErr(spec.Process.Pos(), fmt.Sprintf("процесс '%s' не объявлен", spec.Process.Name))
    }
    // (б) шаг существует в процессе
    if !hasStep(pd, spec.Step.Name) {
        return semErr(spec.Step.Pos(),
            fmt.Sprintf("шаг '%s' не найден в процессе '%s'", spec.Step.Name, spec.Process.Name))
    }
    // (в) тело — как РАСПИСАНИЕ (lenient-scope, D-AU-6): оба контекст-флага false
    return i.checkTriggerBody(td.Body, false, false)
```

`процесс '%s' не объявлен` — переиспользуется из `analyze.go:729` (та же формулировка
`checkRunProcess`). `hasStep` — итерация по `pd.Steps` (как `analyze.go:277/285`).

## Семантика тела (lenient-scope БЕСПЛАТНО)

`checkTriggerBody(td.Body, false, false)` (зеркало расписания, `analyze.go:368`):
- Никакого нового флага `inDeadlineTrigger` НЕ нужно.
- Семпроход НЕ проверяет объявленность плоских Ident → свободный `факт` статической ошибки
  не даёт; резолвится в рантайме против инжекта `InstanceVariables` (B4b).
- `значение`/`событие` в теле → наследуемый контекст-гард TR-VAL-CTX / TR-EVT-CTX (оба false).
- Действия-шага (`исполнитель:`/`срок:`) в теле → наследуемый запрет §TR-7.C.
- Read-only барьер тела — РАНТАЙМ (TR-BODY-RO), отдельной аналитики не требует.

## Диагностики (СемантическаяОшибка — НЕ 28-счёт eval)

Триггерные семантические коды живут в `analyze_trigger_test.go`-семействе (§AU-10.B), НЕ в
`errors_golden_test.go` (len=28). **eval=28 НЕ инкрементится.**

| Вход | Сообщение |
|------|-----------|
| процесс не объявлен | `процесс '<имя>' не объявлен` |
| шаг не найден | `шаг '<шаг>' не найден в процессе '<процесс>'` |
| `значение` в теле | TR-VAL-CTX (наследуемый) |
| `событие` в теле | TR-EVT-CTX (наследуемый) |

## Тесты (B4a-группа)

- Позитив: процесс+шаг объявлены, тело со свободным `факт` → analyze OK (нет статической ошибки).
- Негатив (а): неизв. процесс → `процесс '<имя>' не объявлен` (exact + позиция `Process`).
- Негатив (б): известный процесс, неизв. шаг → `шаг '<шаг>' не найден в процессе '<процесс>'`.
- Негатив (в): `значение`/`событие` в теле → наследуемый контекст-гард срабатывает.
- Замок eval=28: после добавления кейса `errors_golden_test.go` len(seen)==28 (НЕ изменилось).
- Инверсия: убрать проверку (б) → `когда задача просрочена в P.нетшага:` ложно проходит → красный.
- Инверсия: убрать проверку (а) → неизв. процесс ложно проходит → красный.
