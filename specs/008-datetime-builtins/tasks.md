---
description: "Task list — 008-datetime-builtins"
---

# Tasks: 008 — Активация 7 отложенных дата/время builtins

**Input**: Design documents from `specs/008-datetime-builtins/`
**Dev root**: `/Users/denis/dev/ladix` (модуль в `src/`); пути ниже — относительно этого корня.
**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/ (README + 3), quickstart.md, `docs/datetime-builtins-model.md §DB-0…§DB-9` (якорь истины; §DB-6 — инвентарь тест-замков).

**Tests**: ВКЛЮЧЕНЫ. Это рефактор-активация с ОБЯЗАТЕЛЬНОЙ синхронной перелочкой тест-замков:
без инверсии 4 файлов §DB-6 (счётчики реестра, deferred-замки, golden-кейс `SEM-DEFERRED-BUILTIN`,
агрегатный счётчик `errors_golden:206`) гейт `go test ./...` краснеет. Приёмка задаётся table-driven
позитивами (golden-матрица §DB-7) и негативами (4 exact-match строки) — конвенция фич 001–007.

**Organization**: задачи сгруппированы по user story (US1→US4 по приоритету P1→P2). Активация реестра
АТОМАРНА (нельзя полу-активировать `deferredNames`), поэтому фазы — про порядок РАЗРАБОТКИ внутри
одного PR, а не про раздельную поставку. Финальный гейт зелёный достигается только когда код И
инверсия всех тест-замков в дереве.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: можно выполнять параллельно (разные файлы, нет незавершённых зависимостей)
- **[Story]**: US1 / US2 / US3 / US4 (только для фаз стори)
- Точные пути файлов — в каждой задаче

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: базовая готовность ветки и сборки. 0 новых пакетов/зависимостей — расширяется `internal/eval`.

- [ ] T001 Подтвердить ветку `008-datetime-builtins` и зелёный бейзлайн: из `/Users/denis/dev/ladix` выполнить `cd src && go build ./... && go test ./...`; зафиксировать, что рабочее дерево чистое и собирается до начала правок. Подтвердить, что `src/internal/eval/builtins_date.go`, `src/internal/eval/window.go` (`dateToTime`/`AddDate`/`timeToDate`), `src/internal/eval/arith.go` (`mulInt64`) и хелпер `run(t, src)` в `src/internal/eval/helpers_test.go` (инжектит `FixedClock{Дата{2026,5,31}}`) на месте

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: реализация 7 функций в новом файле — ядро, на которое опираются ВСЕ стории и инверсия реестра. ⚠️ Ни одна стори/инверсия счётчиков не завершится до завершения этой фазы.

- [ ] T002 Создать новый файл `src/internal/eval/builtins_duration.go` в стиле `src/internal/eval/builtins_date.go` (тот же шаблон сигнатуры builtin-функции `(i *Interpreter, args []value.Value, pos errors.Position) (value.Value, error)`); добавить локальную (НЕ глобал пакета) карту множителей `var секундВЕдинице = map[string]int64{"сек":1,"мин":60,"час":3600,"дн":86400,"нед":604800}` (без `мес` — он не приводится). Файл — носитель реализаций T003/T004/T005 (ниже). Зависит от T001
- [ ] T003 [P] Реализовать `вчера()` и `завтра()` в `src/internal/eval/builtins_duration.go`: арность 0; `вчера` → `timeToDate(dateToTime(i.now()).AddDate(0,0,-1))`, `завтра` → `… .AddDate(0,0,+1)` (хелперы `window.go`); результат `value.Дата`; «сегодня» ТОЛЬКО через `i.now()` (инжектируемый `Clock`), никогда `time.Now()`; собственных ошибок выполнения нет (только арность, ловится реестром). Зависит от T002
- [ ] T004 [P] Реализовать `длительность(значение, единица)` в `src/internal/eval/builtins_duration.go`: арность 2; порядок валидации СТРОГО арность→тип→единица: (1) тип — `значение:Целое` И `единица:Строка`, иначе ОшибкаТипа `длительность: ожидается Целое и Строка, получено <тип> и <тип>` (показать ОБА `TypeName`, даже если неверен один; `Дробное` — ошибка типа, промоушена нет); (2) единица ∈ {`сек`,`мин`,`час`,`дн`,`нед`,`мес`}, иначе ОшибкаВыполнения `длительность: неизвестная единица «<единица>»` (гильемы); результат `value.Длительность{Amount: значение, Unit: единица}` БЕЗ нормализации; `0`/отрицательное конструируются без ошибки; позиция всех ошибок = `pos` (`CallExpr.Pos()`). Зависит от T002
- [ ] T005 [P] Реализовать 4 конвертера `в_секундах`/`в_минутах`/`в_часах`/`в_днях` в `src/internal/eval/builtins_duration.go` через общий хелпер: арность 1; (1) тип — `d:Длительность`, иначе ОшибкаТипа `<имя>: ожидается Длительность, получено <тип>`; (2) если `d.Unit=="мес"` → ОшибкаВыполнения `<имя>: месяцы не приводятся без даты-якоря` (`AddDate` НЕ вызывается); (3) `totalSec, overflow := mulInt64(d.Amount, секундВЕдинице[d.Unit])`, при overflow → ОшибкаВыполнения `переполнение целого числа` (существующая строка реестра, новой НЕ вводить); (4) целочисленное частное с усечением к нулю: сек→`totalSec`, мин→`/60`, час→`/3600`, дн→`/86400`; результат `value.Целое`; `<имя>` = имя вызванной функции; позиция = `pos`. Зависит от T002

**Checkpoint**: 7 функций реализованы (компилируются, ещё не зарегистрированы) — стории могут начинать позитив/негатив-тесты, активация реестра — в US-фазах.

---

## Phase 3: User Story 1 — Получить относительную дату (вчера/завтра) (Priority: P1) 🎯 MVP

**Goal**: `вчера()`/`завтра()` зарегистрированы как активные `ArityFixed N=0`; при `FixedClock{2026-05-31}` дают `Дата` 2026-05-30 / 2026-06-01; `тип(вчера())` = `"Дата"`.

**Independent Test**: через `run(t, src)` (инжектит `FixedClock{2026-05-31}`) вызвать `вчера()`/`завтра()` и сверить golden-даты и `тип(...)`; НЕ использовать `time.Now`/`SystemClock`.

- [ ] T006 [US1] Зарегистрировать `вчера` и `завтра` явными `add(Builtin{Name:"вчера", Arity:ArityFixed, N:0, Fn:builtinВчера})` / `…"завтра"…N:0…` в `src/internal/eval/builtins.go` (~:88-90, рядом с активными дата-функциями). Зависит от T003
- [ ] T007 [P] [US1] Позитивные golden-тесты `вчера`/`завтра` в `src/internal/eval/duration_builtins_test.go` через хелпер `run(t, src)` (детерминизм: `FixedClock{2026-05-31}`, НЕ `time.Now`): `вчера()` ⇒ `Дата` 2026-05-30; `завтра()` ⇒ `Дата` 2026-06-01 (переход через границу месяца); `тип(вчера())` ⇒ `"Дата"`, `тип(завтра())` ⇒ `"Дата"`. Зависит от T006

**Checkpoint**: US1 функциональна — относительные даты вызываемы и golden-точны.

---

## Phase 4: User Story 2 — Сконструировать длительность из значения и единицы (Priority: P1)

**Goal**: `длительность(значение, единица)` зарегистрирована как активная `ArityFixed N=2`; `длительность(5,"мин") == 5мин` истинно; `0`/отрицательное конструируются без ошибки.

**Independent Test**: через `run(t, src)`: `длительность(5,"мин") == 5мин` ⇒ истина; `длительность(0,"нед")` и `длительность(-3,"дн")` конструируются без ошибки; `длительность(1,"мес")` конструируется (ошибка лишь при `в_*`).

- [ ] T008 [US2] Зарегистрировать `длительность` явным `add(Builtin{Name:"длительность", Arity:ArityFixed, N:2, Fn:builtinДлительность})` в `src/internal/eval/builtins.go` (~:88-90). Зависит от T004
- [ ] T009 [P] [US2] Позитивные тесты `длительность` в `src/internal/eval/duration_builtins_test.go` через `run(t, src)`: `длительность(5,"мин") == 5мин` ⇒ истина (равенство по `{Amount,Unit}`, без нормализации); `длительность(0,"нед")` и `длительность(-3,"дн")` — без ошибки; `длительность(1,"мес")` конструируется без ошибки; (опц.) `длительность(60,"мин") == длительность(1,"час")` ⇒ ложь. Зависит от T008

**Checkpoint**: US2 функциональна — конструктор активен; вход для US3 готов.

---

## Phase 5: User Story 3 — Перевести длительность в число выбранной единицы (Priority: P1)

**Goal**: 4 конвертера `в_*` зарегистрированы как активные `ArityFixed N=1`; матрица 5 единиц × 4 функции golden-точна; усечение к нулю в обе стороны знака.

**Independent Test**: через `run(t, src)` проверить тождества (`в_днях(длительность(1,"нед"))`=7, `в_часах(длительность(1,"дн"))`=24, `в_секундах(длительность(2,"мин"))`=120) и усечение (`в_минутах(длительность(90,"сек"))`=1, `в_часах(длительность(90,"мин"))`=1, `в_днях(длительность(40,"час"))`=1, `в_минутах(длительность(-90,"сек"))`=-1).

- [ ] T010 [US3] Зарегистрировать `в_секундах`/`в_минутах`/`в_часах`/`в_днях` четырьмя явными `add(Builtin{Name:…, Arity:ArityFixed, N:1, Fn:…})` в `src/internal/eval/builtins.go` (~:88-90). Зависит от T005, T008
- [ ] T011 [P] [US3] Позитивные golden-тесты конвертеров в `src/internal/eval/duration_builtins_test.go` через `run(t, src)`: тождества `в_днях(длительность(1,"нед"))`=7 / `в_часах(длительность(1,"дн"))`=24 / `в_секундах(длительность(2,"мин"))`=120; матрица `в_секундах` × множитель (сек=1, мин=60, час=3600, дн=86400, нед=604800); усечение к нулю `в_минутах(длительность(90,"сек"))`=1 / `в_часах(длительность(90,"мин"))`=1 / `в_днях(длительность(40,"час"))`=1 / `в_минутах(длительность(-90,"сек"))`=-1. Зависит от T010

**Checkpoint**: US1+US2+US3 — P1-набор полон (7 функций активны, golden-матрица зелёная).

---

## Phase 6: User Story 4 — Понятная ошибка при неверной единице/типе/месяцах (Priority: P2)

**Goal**: 4 новые exact-match строки ошибок (байт-в-байт, БЕЗ точки, с гильемами «…») наблюдаемы; арность и переполнение переиспользуют существующие строки.

**Independent Test**: через `run(t, src)` вызвать каждую функцию с заведомо неверным аргументом и сверить сообщение об ошибке байт-в-байт.

- [ ] T012 [P] [US4] Негатив-тесты 4 новых строк ошибок в `src/internal/eval/duration_builtins_test.go` (байт-в-байт, гильемы «», без завершающей точки): `длительность(1.5,"час")` ⇒ `длительность: ожидается Целое и Строка, получено Дробное и Строка`; `длительность("5","мин")` ⇒ `длительность: ожидается Целое и Строка, получено Строка и Строка`; `длительность(5,"XYZ")` ⇒ `длительность: неизвестная единица «XYZ»`; `в_секундах(5)` ⇒ `в_секундах: ожидается Длительность, получено Целое`; `в_секундах(длительность(1,"мес"))` ⇒ `в_секундах: месяцы не приводятся без даты-якоря` (и хотя бы один второй конвертер, напр. `в_минутах`, на «мес» — подтвердить `<имя>`-подстановку). Зависит от T010
- [ ] T013 [P] [US4] Тесты переиспользуемых строк (новых НЕ вводить) в `src/internal/eval/duration_builtins_test.go`: нарушение арности `длительность(5)` ⇒ `'длительность' принимает 2 аргументов, передано 1` и `в_секундах()` ⇒ `'в_секундах' принимает 1 аргументов, передано 0` (существующая форма реестра); (опц.) переполнение конвертера на большом `Amount`×множитель ⇒ `переполнение целого числа` (существующая строка). Зависит от T010

**Checkpoint**: US4 — диагностика полна и байт-точна; все 7 функций активны и протестированы позитив+негатив.

---

## Phase 7: Активация реестра + ретайр + синхронная перелочка тест-замков (§DB-6)

**Purpose**: довести реестр до инварианта «35 активных, 0 deferred = 35», ретайрить `SEM-DEFERRED-BUILTIN` в инертный backstop и СИНХРОННО инвертировать все тест-замки §DB-6 (4 файла). ⚠️ Без задач T015–T020 гейт `go test ./...` краснеет — это НЕ опциональная уборка.

> ⚠️ После T014 цикл-заглушек удалён и `deferredNames` пуст: тест `TestBuiltinDeferredAll` становится вакуумно-зелёным, `TestDeferredDateBuiltinStillDeferred` падает, golden-кейс `SEM-DEFERRED-BUILTIN` исчезает, а оба агрегатных счётчика (`TestBuiltinRegistryClosed` и `errors_golden:206`) расходятся с реальностью. Все инверсии co-land с T014.

### Активация реестра и ретайр диагностики

- [ ] T014 [US] Активировать реестр в `src/internal/eval/builtins.go`: `deferredNames` (:49) → пустой `[]string{}`; УДАЛИТЬ цикл, который добавлял заглушки по `deferredNames` (:96-99); инвариант-комментарий (:54-55) переформулировать в «РОВНО 35 активных, 0 deferred = 35» (D-D, §DB-6); комментарий поля `Builtin.Deferred` (:30) — пометить «инертный backstop, механизм НЕ удалён» (см. T015). Зависит от T006, T008, T010
- [ ] T015 [US] Ретайр `SEM-DEFERRED-BUILTIN` как ИНЕРТНОГО backstop'а: ветку `if b.Deferred` в `src/internal/eval/analyze.go:592` и ветку `if b.Deferred` в `src/internal/eval/call.go:34` ОСТАВИТЬ на месте + обновить комментарии («backstop: при пустом `deferredNames` недостижим; поле `Deferred` и механизм НЕ удаляются» — D-DB-якорь). Поле `Builtin.Deferred` и механизм НЕ удалять. Зависит от T014

### Синхронная инверсия тест-замков (§DB-6 — 4 файла; КАЖДЫЙ отдельной задачей)

- [ ] T016 [US] **Счётчики реестра** в `src/internal/eval/builtins_test.go`: `TestBuiltinRegistryClosed` — обновить ассерты `active 28→35` и `deferred 7→0` (общее `len(m)` остаётся 35). ⚠️ ОТДЕЛЬНАЯ ОБЯЗАТЕЛЬНАЯ задача: без правки этих двух чисел гейт `go test` красный. Зависит от T014
- [ ] T017 [US] **Deferred-замок и позитивы** в `src/internal/eval/builtins_test.go`: УДАЛИТЬ `TestBuiltinDeferredAll` (стал вакуумно-зелёным на пустом `deferredNames`); добавить позитивные тесты 7 функций через хелпер `run(t, ...)` (минимальная sanity-проверка вызываемости каждого имени, если не покрыто T007/T009/T011 — без дублирования матрицы). Зависит от T014
- [ ] T018 [US] **Deferred-замок дат** в `src/internal/eval/duration_builtins_test.go`: УДАЛИТЬ `TestDeferredDateBuiltinStillDeferred` (7 функций больше не deferred); убедиться, что позитивы/негативы T007/T009/T011/T012/T013 в этом файле покрывают вчера/завтра/конвертеры/усечение/мес-ошибку через `run(t, ...)`. Зависит от T014
- [ ] T019 [US] **Семантический deferred-кейс** в `src/internal/eval/analyze_test.go`: УДАЛИТЬ кейс «deferred-встроенная» (`печать(вчера())`, ожидавший `SEM-DEFERRED-BUILTIN`) — после активации `вчера` вызывается чисто. Зависит от T014
- [ ] T020 [US] **Golden-кейс + агрегатный счётчик** в `src/internal/eval/errors_golden_test.go`: УДАЛИТЬ golden-кейс `SEM-DEFERRED-BUILTIN`; декрементировать агрегатный счётчик `:206` `len(seen) != 29` → `28`; поправить докстринг (`:7`) под новое число кодов. ⚠️ ОТДЕЛЬНАЯ задача с явным счётчиком 29→28: без декремента гейт `go test` красный. Зависит от T014, T015

**Checkpoint**: реестр на инварианте 35/0; `SEM-DEFERRED-BUILTIN` инертен; все 4 файла §DB-6 инвертированы; `go test ./...` зелёный.

---

## Phase 8: Polish & Cross-Cutting Concerns

- [ ] T021 [P] Подтвердить инвариант реестра программно: `len(deferredNames)==0` и счётчик активных = 35 (через `TestBuiltinRegistryClosed`); сверить тексты 4 новых строк ошибок байт-в-байт с `contracts/` и `docs/datetime-builtins-model.md §DB-5` (гильемы «», без точки)
- [ ] T022 [P] **НЕ ТРОГАТЬ (напоминание-задача, §DB-6)**: убедиться, что НЕ изменены `src/internal/value/equal_test.go`, `src/internal/eval/analyze_decl_test.go`, `src/internal/eval/analyze_time_test.go`, `src/internal/parser/parse_decl.go`, `src/cmd/ladix/serve.go`, каталог `examples/` — фича их не затрагивает; `git status` по этим путям чист
- [ ] T023 Финальный гейт: из `/Users/denis/dev/ladix` `cd src && go build ./... && go vet ./... && go test ./... -count=1` зелёный; `gofmt -l src` пуст (форматирование чисто). БЕЗ добавления нового `.ladix`-примера (D-E). БЕЗ правки shared-доков (§DB-8 — работа архитектора при посадке кода)

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: без зависимостей
- **Foundational (Phase 2)**: после Setup — БЛОКИРУЕТ все стории (реализация 7 функций в новом файле)
- **US1 (Phase 3)**: после Foundational
- **US2 (Phase 4)**: после Foundational (регистрация `длительность`)
- **US3 (Phase 5)**: после US2 (конвертеры тестируются через `длительность(...)` — фактический вход)
- **US4 (Phase 6)**: после US3 (негативы поверх всех 7 активных функций)
- **Активация+ретайр+замки (Phase 7)**: после регистрации всех 7 (T006/T008/T010); инверсии co-land с T014
- **Polish (Phase 8)**: после Phase 7

> ⚠️ Активация реестра АТОМАРНА: цикл-заглушек удаляется и `deferredNames` пустеет за один шаг (T014).
> Все 4 инверсии тест-замков §DB-6 (T016–T020) и ретайр backstop'а (T015) ОБЯЗАНЫ co-land с T014 —
> иначе финальный гейт T023 `go test ./...` недостижим. В частности: счётчики `TestBuiltinRegistryClosed`
> (28→35 / 7→0, T016) и агрегатный счётчик `errors_golden:206` (29→28, T020) — две отдельные обязательные
> правки чисел, каждая своей задачей.

### Within Each User Story

- Регистрация (`add` в `builtins.go`) → позитив/негатив-тесты через `run(t, src)`
- US3 опирается на конструктор US2 (`длительность(...)` строит вход для `в_*`)
- US4 опирается на все 7 активных функций (негативы) — после US3

### Parallel Opportunities

- Foundational: T003 / T004 / T005 параллельны (один файл, но независимые функции — раздельные коммиты)
- US-тесты: T007 ∥ T009 ∥ T011 (один тестовый файл, разные тест-функции — независимые дописки)
- US4: T012 ∥ T013 (разные тест-функции)
- Phase 7: T016–T020 — разные файлы/тест-функции, все после T014 (co-land)
- Polish: T021 ∥ T022

---

## Parallel Example: Phase 2 (Foundational)

```bash
# После T002 (файл + карта множителей) — 3 функции независимы:
Task: "T003 Реализовать вчера()/завтра() в src/internal/eval/builtins_duration.go"
Task: "T004 Реализовать длительность(...) в src/internal/eval/builtins_duration.go"
Task: "T005 Реализовать 4 конвертера в_* в src/internal/eval/builtins_duration.go"
```

## Parallel Example: Phase 7 (инверсия тест-замков §DB-6 — все после T014)

```bash
# 4 файла §DB-6 + ретайр backstop'а — co-land с T014:
Task: "T016 Счётчики TestBuiltinRegistryClosed 28→35 / 7→0 в builtins_test.go"
Task: "T017 Удалить TestBuiltinDeferredAll + позитивы 7 функций в builtins_test.go"
Task: "T018 Удалить TestDeferredDateBuiltinStillDeferred в duration_builtins_test.go"
Task: "T019 Удалить кейс печать(вчера()) в analyze_test.go"
Task: "T020 Удалить golden SEM-DEFERRED-BUILTIN + счётчик 29→28 в errors_golden_test.go"
```

---

## Implementation Strategy

### MVP (P1)

1. Phase 1 Setup → Phase 2 Foundational (7 функций реализованы в `builtins_duration.go`)
2. **US1** (Phase 3): регистрация + golden `вчера`/`завтра` через `FixedClock`
3. **US2** (Phase 4): регистрация + конструктор `длительность`
4. **US3** (Phase 5): регистрация + матрица конвертеров `в_*`
5. P1-набор (US1+US2+US3) = поставляемый MVP фичи (7 функций активны)

### Incremental Delivery

1. Foundational → реализация готова (компилируется)
2. + US1 → относительные даты вызываемы (golden)
3. + US2 → конструктор длительности активен
4. + US3 → конвертеры активны (матрица + усечение)
5. + US4 → байт-точные диагностики (4 новые строки)
6. + Phase 7 → реестр на инварианте 35/0, backstop инертен, все замки §DB-6 инвертированы, гейт зелёный

### Границы 008 (НЕ делать здесь — отложено в v2, §DB-9)

Операторная арифметика дат/длительностей (`Дата±Длит`, `Дата−Дата`, `Длит±Длит`, `Длит*X`) ОСТАЁТСЯ
deferred (D-A=A). Нормализация единиц (`1час≠60мин`) — нет. Конверсия `мес` через дату-якорь — нет.
Удаление поля/механизма `Builtin.Deferred` — нет (остаётся инертным backstop'ом). Новый `.ladix`-пример
— нет (D-E). Правка shared-доков (`docs/stdlib.md`, `docs/eval-model.md`, `docs/source-metric-model.md`,
`SPEC.md`, `docs/engine-model.md`) — нет (§DB-8, работа архитектора при посадке кода).

---

## Notes

- [P] = разные файлы/тест-функции, нет незавершённых зависимостей
- Тесты включены: приёмка определена golden-матрицей §DB-7 (позитив) и 4 exact-match строками (негатив)
- **Детерминизм**: позитив-тесты дат — ТОЛЬКО через `run(t, src)` (инжектит `FixedClock{2026-05-31}`); НЕ `time.Now`/`SystemClock`. Golden: `вчера()`→2026-05-30, `завтра()`→2026-06-01, множители (сек=1/мин=60/час=3600/дн=86400/нед=604800), усечение (`в_минутах(90сек)`=1, `в_часах(90мин)`=1, `в_днях(40час)`=1, `в_минутах(-90сек)`=-1)
- **4 новые строки ошибок** (байт-в-байт, без точки, гильемы «»): `длительность: ожидается Целое и Строка, получено <тип> и <тип>` · `длительность: неизвестная единица «<единица>»` · `<имя>: ожидается Длительность, получено <тип>` · `<имя>: месяцы не приводятся без даты-якоря`. Арность и переполнение — существующие строки реестра, новых НЕ вводить
- **Два счётчика** инвертируются явными отдельными задачами: `TestBuiltinRegistryClosed` (28→35 / 7→0, T016) и агрегатный `errors_golden:206` (29→28, T020) — без них гейт T023 недостижим
- **`Builtin.Deferred` НЕ удаляется** — остаётся инертным backstop'ом (T015); механизм цел, цикл-заглушек удалён (T014)
- Коммит после каждой задачи или логической группы; инверсии замков §DB-6 co-land с T014
