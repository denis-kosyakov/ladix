# Фикстура с ПАРС-ошибкой: complete компилирует файл (лексер→парсер→Analyze) до
# запуска движка; компиляция падает на парсере → канон §13, exit 1. Это НЕ
# examples/ошибка.ladix (та проходит компиляцию чисто и падает лишь в рантайме,
# а complete top-level не исполняет). Незавершённое присваивание — синтаксис битый.
процесс онбординг(сотрудник):
    шаг завести_доступы:
        уведомить ИТ("создать учётку для " + сотрудник)

пусть id =
