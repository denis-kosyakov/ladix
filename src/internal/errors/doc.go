// Package errors содержит явные типы ошибок и общую Position для всего
// интерпретатора Ladix (конституция III/IV).
//
// На этом шаге (фича 001-lexer-tokens) реализован объём, нужный лексеру:
// Position, LexError, агрегат ErrorList и зарезервированный Warning, который
// лексер НЕ порождает (guardrail 5 / A5). Прочие категории (ParseError и т.п.)
// добавятся на следующих шагах.
//
// Пакет ЛИСТОВОЙ: внутри проекта ни на кого не опирается (ARCHITECTURE §2.1),
// поэтому граф зависимостей lexer → errors ацикличен.
package errors
