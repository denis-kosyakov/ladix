// Package value — листовая модель рантайм-значений Ladix (фича 003).
//
// Зависит ТОЛЬКО от стандартной библиотеки Go (конституция VII, Guardrail 1):
// не импортирует ни ast, ни errors, ни eval. Это разрывает потенциальный цикл
// eval↔store (ARCHITECTURE §2.1, вариант A) и даёт нейтральную поверхность под
// будущий internal/store (фича 004+).
//
// Интерфейс Value реализуют РОВНО 10 конкретных типов; в чистом 003
// конструируются 6 (Целое/Дробное/Строка/Булево/Пусто/Список). Запись
// смоделирована полностью (чтение полей, равенство, печать с сохранением
// порядка), но не порождается; Дата/Длительность/Период определены, но не
// конструируются (deferred-ворота закрыты, §1.2).
//
// Публичные Go-имена типов — канонические русские (Целое/Дробное/…), они же
// возвращаются из TypeName() для тип(x) и подстановки <тип> в сообщениях
// об ошибках. Функция значением НЕ является (ровно 10 типов, §2.3).
package value
