// Package ast определяет узлы абстрактного синтаксического дерева Ladix для
// подмножества B (императивное ядро): выражения, statements, блоки, FunctionDecl
// и зарезервированные узлы StepAction/RunProcessExpr.
//
// Пакет ЛИСТОВОЙ: он несёт СВОЮ локальную Position и НЕ импортирует
// internal/errors (D1, guardrail 1, конституция IV/VII). Дерево — стабильный
// контракт для будущего eval (фича 003); см. specs/002-parser-ast/contracts/ast.md.
//
// Границы: пакет НЕ выполняет разбор, НЕ делает семантику (типы/области/арность)
// и НЕ материализует узел GroupExpr — скобки сворачиваются парсером (D5).
package ast
