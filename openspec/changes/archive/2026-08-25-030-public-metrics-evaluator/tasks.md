## 1. Подготовка

- [x] 1.1 Создать ветку `030-public-metrics-evaluator` от `master`
- [x] 1.2 Прочитать `docs/eval-model.md` (§8.3), `docs/source-metric-model.md`
      (§SM-7, §SM-8, §SM-9), `docs/module-contract.md` (§MC-1, §MC-4, §MC-5);
      при противоречии с design.md — остановиться и спросить

## 2. Tests-first: контракт фасада (Принцип VI)

- [x] 2.1 Табличные тесты фасада `metrics`: вычисление метрики над записями
      потребителя (счёт/фильтр/окно периода) с фиксированной датой — по
      сценариям «Исполнение метрики над данными потребителя»
- [x] 2.2 Тест «Совпадение с reference-CLI»: один вход → фасад и путь
      `ladix metric` (файловый источник) дают идентичный результат
- [x] 2.3 Негативные тесты диагностик: деление на ноль в `где:`,
      не-Булево `где:`, недопустимая каноническая строка — дословные тексты
      реестров + текст «недопустимое каноническое выражение», позиции в рунах
- [x] 2.4 Тесты детерминизма: повторный запуск байт-в-байт; окно периода от
      инжектированной даты (две разные даты → два разных окна)
- [x] 2.5 Тесты потолка сложности: глубина 100 проходит / 101 — отказ;
      узлы 10000 проходят / 10001 — отказ; дословные тексты диагностик

## 3. Реализация пакета `metrics`

- [x] 3.1 Каркас пакета: сигнатура фасада по Д-1/Д-3/Д-5
      (`ir.Metric` + `[]map[string]any` + опции с датой → результат,
      `[]ir.Diagnostic`, `error`), recover-барьер на границе
- [x] 3.2 Разбор канонических строк внутренними `lexer`/`parser`;
      маппинг записей потребителя по JSON-семантике §9.3/§9.4 (Д-2),
      переиспользуя внутренний путь загрузчика
- [x] 3.3 Потолок сложности на expr-AST до вычисления (Д-4): глубина/узлы,
      диагностики дословно по дельте спеки
- [x] 3.4 Вызов конвейера §SM-8 с инжектированными записями и `FixedClock`;
      при необходимости — НОВАЯ внутренняя точка входа в `internal/eval`
      без изменения существующих путей (Д-6)
- [x] 3.5 Прогнать тесты группы 2 до зелёного

## 4. Замки и мутпробы

- [x] 4.1 Расширить `boundary_test.go`: пакет `metrics` в списке T3;
      мутпроба — временно импортировать `internal/store` в `metrics` и
      убедиться, что страж краснеет (изменение не коммитить)
- [x] 4.2 Замок «без wall-clock»: тест, грепающий пакет `metrics` на
      `time.Now`; мутпроба — вставить `time.Now()` и убедиться в красноте
- [x] 4.3 Замок «Compile не ограничивает»: программа с выражением глубже 100
      компилируется в тот же IR; мутпроба — перенести потолок в Compile и
      убедиться, что замок краснеет
- [x] 4.4 Корпус golden result-фикстур `(программа + вход + дата) → результат`
      (минимум: все виды агрегатов, окно периода, пустой результат §10.5);
      мутпроба — изменить одну фикстуру и убедиться в красноте
- [x] 4.5 Замок неизменности `ladix`/`ir`: убедиться, что регресс-гейт корпуса
      029 и `ir.SchemaVersion == 1` зелёные без правок
- [x] 4.6 Замок README-сниппета: `Example_readme` (Output: 3800) + тест
      выравнивания текста README с зеркалом сниппета

## 5. Doc-sync и финал

- [x] 5.1 Doc-sync по списку design.md: `docs/module-contract.md` (§MC-1
      «ровно три», новый § контракта), `docs/eval-model.md`,
      `docs/diagnostics-model.md` (4 новых текста), `README.md`,
      `ARCHITECTURE.md`, CHANGELOG v0.2.0
- [x] 5.2 Финал: `gofmt` чист, `go vet ./...` без замечаний,
      `go build -o ladix ./cmd/ladix` и `go test ./...` зелёные;
      сверка границ диффа с proposal (пустые диффы действительно пусты)
